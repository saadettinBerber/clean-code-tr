window.PAGE({"id": "page-46", "page": 46, "pdf_page": 77, "chapter": {"num": 3, "en": "Functions", "tr": "Fonksiyonlar"}, "section": {"en": "Output Arguments / Command Query Separation", "tr": "Çıkış Argümanları / Komut Sorgu Ayrımı"}, "title": {"en": "Prefer Exceptions to Returning Error Codes", "tr": "Hata Kodları Yerine İstisnalar"}, "blocks": [{"type": "para", "sentences": [{"en": "could try to resolve this by renaming the set function to `setAndCheckIfExists`, but that doesn’t much help the readability of the if statement.", "tr": "Bunu, `set` fonksiyonunu `setAndCheckIfExists` olarak yeniden adlandırarak çözmeye çalışabiliriz, ama bu `if` ifadesinin okunabilirliğine pek yardımcı olmaz."}, {"en": "The real solution is to separate the command from the query so that the ambiguity cannot occur.", "tr": "Gerçek çözüm, komutu sorgudan ayırarak bu belirsizliğin ortaya çıkmasını engellemektir."}]}, {"type": "code", "lang": "java", "code": "if (attributeExists(\"username\")) {\n  setAttribute(\"username\", \"unclebob\");\n  ...\n}"}, {"type": "heading", "level": 1, "en": "Prefer Exceptions to Returning Error Codes", "tr": "Hata Kodları Yerine İstisnaları Tercih Edin"}, {"type": "para", "sentences": [{"en": "Returning error codes from command functions is a subtle violation of command query separation.", "tr": "Komut fonksiyonlarından hata kodu (Error Code) döndürmek, komut sorgu ayrımının ince bir ihlalidir."}, {"en": "It promotes commands being used as expressions in the predicates of if statements.", "tr": "Bu, komutların `if` ifadelerinin yüklemlerinde (Predicate) birer ifade olarak kullanılmasını teşvik eder."}]}, {"type": "code", "lang": "java", "code": "if (deletePage(page) == E_OK)"}, {"type": "para", "sentences": [{"en": "This does not suffer from verb/adjective confusion but does lead to deeply nested structures.", "tr": "Bu, fiil/sıfat karışıklığına yol açmaz ama derinlemesine iç içe geçmiş yapılara neden olur."}, {"en": "When you return an error code, you create the problem that the caller must deal with the error immediately.", "tr": "Bir hata kodu döndürdüğünüzde, çağıranın hatayla hemen ilgilenmek zorunda kalması sorununu yaratırsınız."}]}, {"type": "code", "lang": "java", "code": "if (deletePage(page) == E_OK) {\n  if (registry.deleteReference(page.name) == E_OK) {\n    if (configKeys.deleteKey(page.name.makeKey()) == E_OK){\n      logger.log(\"page deleted\");\n    } else {\n      logger.log(\"configKey not deleted\");\n    }\n  } else {\n    logger.log(\"deleteReference from registry failed\");\n  }\n} else {\n  logger.log(\"delete failed\");\n  return E_ERROR;\n}"}, {"type": "para", "sentences": [{"en": "On the other hand, if you use exceptions instead of returned error codes, then the error processing code can be separated from the happy path code and can be simplified:", "tr": "Öte yandan, döndürülen hata kodları yerine istisnalar kullanırsanız, hata işleme kodu mutlu yoldan (Happy Path) ayrılabilir ve sadeleştirilebilir:"}]}, {"type": "code", "lang": "java", "code": "try {\n  deletePage(page);\n  registry.deleteReference(page.name);\n  configKeys.deleteKey(page.name.makeKey());\n}\ncatch (Exception e) {\n  logger.log(e.getMessage());\n}"}, {"type": "heading", "level": 2, "en": "Extract Try/Catch Blocks", "tr": "Try/Catch Bloklarını Çıkarın"}, {"type": "para", "sentences": [{"en": "`Try/catch` blocks are ugly in their own right.", "tr": "`Try/catch` blokları kendi başlarına çirkindir."}, {"en": "They confuse the structure of the code and mix error processing with normal processing.", "tr": "Kodun yapısını karmaşıklaştırır ve hata işlemeyi normal işlemle birbirine karıştırır."}, {"en": "So it is better to extract the bodies of the try and catch blocks out into functions of their own.", "tr": "Bu nedenle `try` ve `catch` bloklarının gövdelerini kendi fonksiyonlarına çıkarmak daha iyidir."}]}], "concepts": [{"id": "command-query-separation-fix", "title": {"en": "Command Query Separation", "tr": "Komut Sorgu Ayrımı (Command Query Separation)"}, "summary": {"en": "A function should either do something or answer something, never both — otherwise its name becomes ambiguous when used inside a condition.", "tr": "Bir fonksiyon ya bir şey yapmalı ya da bir soruyu yanıtlamalı, asla ikisini birden yapmamalı — aksi halde bir koşulun içinde kullanıldığında adı belirsizleşir."}, "bad": {"lang": "java", "code": "public boolean setUsername(String name) {\n    if (name == null) return false;\n    this.username = name;\n    return true;\n}\n\nif (setUsername(\"bob\")) {\n    // was it already \"bob\", or did we just set it?\n}", "why": {"en": "The name `setUsername` reads as a command (an action), but its boolean return value forces readers to interpret it as a query too. It is unclear from the call site alone.", "tr": "`setUsername` ismi bir komut (eylem) gibi okunur, ama boolean dönüş değeri okuyucuyu bunu aynı zamanda bir sorgu gibi yorumlamaya zorlar. Çağrı noktasından anlam çıkarmak zordur."}}, "good": {"lang": "java", "code": "public void setUsername(String name) {\n    if (name == null) throw new IllegalArgumentException(\"name is null\");\n    this.username = name;\n}\n\npublic boolean hasUsername() {\n    return this.username != null;\n}\n\nsetUsername(\"bob\");\nif (hasUsername()) {\n    // clearly a question, asked after the command\n}", "why": {"en": "Splitting the command (`setUsername`) from the query (`hasUsername`) removes the ambiguity entirely — each function has exactly one responsibility.", "tr": "Komutu (`setUsername`) sorgudan (`hasUsername`) ayırmak belirsizliği tamamen ortadan kaldırır — her fonksiyonun tam olarak tek bir sorumluluğu vardır."}}, "tip": {"en": "If a function's name reads like both an action and a question, it is doing two jobs — split it into two functions.", "tr": "Bir fonksiyonun adı hem bir eylem hem de bir soru gibi okunuyorsa, o fonksiyon iki iş yapıyordur — onu iki fonksiyona bölün."}}, {"id": "exceptions-over-error-codes", "title": {"en": "Prefer Exceptions to Error Codes", "tr": "Hata Kodları Yerine İstisnalar (Prefer Exceptions to Error Codes)"}, "summary": {"en": "Returning error codes forces the caller to check them immediately, which pushes error handling into the same flow as the happy path and encourages deep nesting.", "tr": "Hata kodu döndürmek, çağıranı bu kodu hemen kontrol etmeye zorlar; bu da hata yönetimini mutlu yolla (happy path) aynı akışa sokar ve derin iç içe geçmeye yol açar."}, "bad": {"lang": "python", "code": "def save_file(path, data):\n    result = write_to_disk(path, data)\n    if result == STATUS_OK:\n        result = update_index(path)\n        if result == STATUS_OK:\n            return STATUS_OK\n        else:\n            return STATUS_INDEX_ERROR\n    else:\n        return STATUS_WRITE_ERROR", "why": {"en": "Every step must be checked against a status code before the next can run, and the caller has to repeat that check. The nesting grows with every extra step.", "tr": "Bir sonraki adım çalışmadan önce her adımın bir durum koduna karşı kontrol edilmesi gerekir ve çağıran da bu kontrolü tekrarlamak zorundadır. Her ek adımla iç içe geçme derinleşir."}}, "good": {"lang": "python", "code": "def save_file(path, data):\n    try:\n        write_to_disk(path, data)\n        update_index(path)\n    except IOError as e:\n        logger.error(f\"Failed to save file: {e}\")\n        raise", "why": {"en": "The happy path reads as a straight, flat sequence; error handling lives in exactly one place instead of being repeated after every call.", "tr": "Mutlu yol, düz ve tek parça bir sıra olarak okunur; hata yönetimi her çağrıdan sonra tekrarlanmak yerine tek bir yerde toplanır."}}, "tip": {"en": "If you find yourself checking a return value against a status constant, ask whether an exception would let you delete that check entirely.", "tr": "Bir dönüş değerini bir durum sabitiyle karşılaştırdığınızı fark ederseniz, bir istisnanın bu kontrolü tamamen ortadan kaldırıp kaldırmayacağını sorgulayın."}}, {"id": "extract-try-catch-bodies", "title": {"en": "Extract Try/Catch Bodies", "tr": "Try/Catch Gövdelerini Çıkarma (Extract Try/Catch Bodies)"}, "summary": {"en": "A try/catch block should coordinate error flow only; the actual work belongs in its own function so normal processing and error processing don't mix.", "tr": "Bir try/catch bloğu yalnızca hata akışını yönetmelidir; asıl iş kendi fonksiyonunda olmalıdır, böylece normal işlem ile hata işlemi birbirine karışmaz."}, "bad": {"lang": "javascript", "code": "function processPayment(order) {\n    try {\n        const charge = gateway.charge(order.total, order.card);\n        order.status = \"paid\";\n        order.transactionId = charge.id;\n        db.save(order);\n        emailReceipt(order);\n    } catch (err) {\n        logger.error(\"Payment failed\", err);\n        order.status = \"failed\";\n        db.save(order);\n        throw new PaymentError(err.message);\n    }\n}", "why": {"en": "The success path and the failure path are tangled inside one function, making it hard to see either one clearly.", "tr": "Başarı yolu ile başarısızlık yolu tek bir fonksiyonun içinde birbirine dolanmıştır; ikisini de net bir şekilde görmek zordur."}}, "good": {"lang": "javascript", "code": "function processPayment(order) {\n    try {\n        chargeAndRecordOrder(order);\n    } catch (err) {\n        handlePaymentFailure(order, err);\n    }\n}\n\nfunction chargeAndRecordOrder(order) {\n    const charge = gateway.charge(order.total, order.card);\n    order.status = \"paid\";\n    order.transactionId = charge.id;\n    db.save(order);\n    emailReceipt(order);\n}\n\nfunction handlePaymentFailure(order, err) {\n    logger.error(\"Payment failed\", err);\n    order.status = \"failed\";\n    db.save(order);\n    throw new PaymentError(err.message);\n}", "why": {"en": "`processPayment` now reads as pure error-flow coordination, while `chargeAndRecordOrder` and `handlePaymentFailure` can each be understood — and tested — in isolation.", "tr": "`processPayment` artık salt hata akışı koordinasyonu gibi okunur; `chargeAndRecordOrder` ve `handlePaymentFailure` ise ayrı ayrı anlaşılabilir ve test edilebilir."}}, "tip": {"en": "Apply Extract Method to both the try body and the catch body — the outer function should do nothing but coordinate the two.", "tr": "Metot Çıkarma (Extract Method) tekniğini hem try gövdesine hem de catch gövdesine uygulayın — dış fonksiyon yalnızca ikisini koordine etsin."}}]});
