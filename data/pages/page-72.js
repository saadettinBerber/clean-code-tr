window.PAGE({"id": "page-72", "page": 72, "pdf_page": 103, "chapter": {"num": 4, "en": "Comments", "tr": "Yorumlar"}, "section": {"en": "GeneratePrimes → PrimeGenerator (Refactored)", "tr": "GeneratePrimes → PrimeGenerator (Yeniden Düzenlenmiş)"}, "title": {"en": "Listing 4-7 (cont.) & Listing 4-8 PrimeGenerator", "tr": "Listing 4-7 (devam) ve Listing 4-8 PrimeGenerator"}, "blocks": [{"type": "code", "lang": "java", "code": "      // initialize array to true.\n      for (i = 0; i < s; i++)\n        f[i] = true;\n\n      // get rid of known non-primes\n      f[0] = f[1] = false;\n\n      // sieve\n      int j;\n      for (i = 2; i < Math.sqrt(s) + 1; i++)\n      {\n        if (f[i]) // if i is uncrossed, cross its multiples.\n        {\n          for (j = 2 * i; j < s; j += i)\n            f[j] = false; // multiple is not prime\n        }\n      }\n\n      // how many primes are there?\n      int count = 0;\n      for (i = 0; i < s; i++)\n      {\n        if (f[i])\n          count++; // bump count.\n      }\n\n      int[] primes = new int[count];\n\n      // move the primes into the result\n      for (i = 0, j = 0; i < s; i++)\n      {\n        if (f[i])             // if prime\n          primes[j++] = i;\n      }\n\n      return primes;  // return the primes\n    }\n    else // maxValue < 2\n      return new int[0]; // return null array if bad input.\n  }\n}"}, {"type": "caption", "en": "In Listing 4-8 you can see a refactored version of the same module. Note that the use of comments is significantly restrained. There are just two comments in the whole module. Both comments are explanatory in nature.", "tr": "Listing 4-8'de aynı modülün yeniden düzenlenmiş sürümünü görebilirsiniz. Yorumların kullanımı belirgin biçimde kısıtlanmış olduğuna dikkat edin. Tüm modülde yalnızca iki yorum vardır. Her iki yorum da açıklayıcı niteliktedir."}, {"type": "para", "sentences": [{"en": "Listing 4-8 PrimeGenerator.java (refactored)", "tr": "Listing 4-8 PrimeGenerator.java (yeniden düzenlenmiş)"}]}, {"type": "code", "lang": "java", "code": "/**\n * This class Generates prime numbers up to a user specified\n * maximum.  The algorithm used is the Sieve of Eratosthenes.\n * Given an array of integers starting at 2:\n * Find the first uncrossed integer, and cross out all its"}], "concepts": [{"id": "restrained-comments-refactor", "title": {"en": "Restrained Comments after Refactoring", "tr": "Yeniden Düzenleme Sonrası Ölçülü Yorumlar"}, "summary": {"en": "Listing 4-8 shows the same prime sieve with comments cut to two explanatory notes; structure and names carry the rest of the documentation.", "tr": "Listing 4-8 aynı asal kalburunu (prime sieve) yorumları iki açıklayıcı nota indirerek gösterir; yapının ve isimlerin geri kalanı dokümantasyonu taşır."}, "bad": {"lang": "java", "code": "// declarations\nint s = maxValue + 1; // size of array\nboolean[] f = new boolean[s]; // array of flags", "why": {"en": "Every line has a noisy inline comment that restates the code.", "tr": "Her satır kodu tekrar eden gürültülü bir satır içi yoruma sahiptir."}}, "good": {"lang": "java", "code": "// Every multiple has a prime factor <= sqrt(size),\n// so we don't have to cross out multiples larger than that.\ndouble iterationLimit = Math.sqrt(crossedOut.length);", "why": {"en": "One explanatory comment clarifies a non-obvious algorithmic choice that names alone cannot.", "tr": "Tek bir açıklayıcı yorum, isimlerin tek başına veremeyeceği bariz olmayan algoritmik bir seçimi netleştirir."}}, "tip": {"en": "After refactoring, keep only comments that explain why — not what.", "tr": "Yeniden düzenlemeden (refactoring) sonra yalnızca neden'i açıklayan yorumları tutun — ne'yi değil."}}]});
