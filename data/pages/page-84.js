window.PAGE({"id": "page-84", "page": 84, "pdf_page": 115, "chapter": {"num": 5, "en": "Formatting", "tr": "Biçimlendirme"}, "section": {"en": "Vertical Formatting", "tr": "Dikey Biçimlendirme (Vertical Formatting)"}, "title": {"en": "Conceptual Affinity and Vertical Ordering", "tr": "Kavramsal Yakınlık ve Dikey Sıralama"}, "blocks": [{"type": "para", "sentences": [{"en": "Conceptual Affinity.", "tr": "Kavramsal Yakınlık (Conceptual Affinity)."}, {"en": "Certain bits of code want to be near other bits.", "tr": "Bazı kod parçaları başka parçaların yakınında olmak ister."}, {"en": "They have a certain conceptual affinity.", "tr": "Aralarında belirli bir kavramsal yakınlık vardır."}, {"en": "The stronger that affinity, the less vertical distance there should be between them.", "tr": "Bu yakınlık ne kadar güçlüyse, aralarındaki dikey mesafe (vertical distance) de o kadar az olmalıdır."}]}, {"type": "image", "src": "imageFile1.png"}, {"type": "para", "sentences": [{"en": "As we have seen, this affinity might be based on a direct dependence, such as one function calling another, or a function using a variable.", "tr": "Gördüğümüz gibi bu yakınlık, bir fonksiyonun başka birini çağırması ya da bir fonksiyonun bir değişkeni kullanması gibi doğrudan bir bağımlılığa (dependency) dayanabilir."}, {"en": "But there are other possible causes of affinity.", "tr": "Ancak yakınlığın başka olası nedenleri de vardır."}, {"en": "Affinity might be caused because a group of functions perform a similar operation.", "tr": "Yakınlık, bir grup fonksiyonun benzer bir işlem yapmasından da kaynaklanabilir."}, {"en": "Consider this snippet of code from Junit 4.3.1:", "tr": "JUnit 4.3.1'den alınan şu kod parçasına bir bakın:"}]}, {"type": "code", "lang": "java", "code": "public class Assert {\n  static public void assertTrue(String message, boolean condition) {\n    if (!condition)\n      fail(message);\n  }\n\n  static public void assertTrue(boolean condition) {\n    assertTrue(null, condition);\n  }\n\n  static public void assertFalse(String message, boolean condition) {\n    assertTrue(message, !condition);\n  }\n\n  static public void assertFalse(boolean condition) {\n    assertFalse(null, condition);\n  }\n..."}, {"type": "para", "sentences": [{"en": "These functions have a strong conceptual affinity because they share a common naming scheme and perform variations of the same basic task.", "tr": "Bu fonksiyonlar arasında güçlü bir kavramsal yakınlık vardır, çünkü ortak bir isimlendirme şemasını paylaşırlar ve aynı temel görevin farklı çeşitlemelerini yerine getirirler."}, {"en": "The fact that they call each other is secondary.", "tr": "Birbirlerini çağırıyor olmaları ise ikinci planda kalır."}, {"en": "Even if they didn’t, they would still want to be close together.", "tr": "Birbirlerini çağırmasalardı bile yine de birbirlerine yakın durmak isterlerdi."}]}, {"type": "heading", "level": 2, "en": "Vertical Ordering", "tr": "Dikey Sıralama (Vertical Ordering)"}, {"type": "para", "sentences": [{"en": "In general we want function call dependencies to point in the downward direction.", "tr": "Genel olarak fonksiyon çağrı bağımlılıklarının (function call dependencies) aşağı yönü göstermesini isteriz."}, {"en": "That is, a function that is called should be below a function that does the calling.<sup>2</sup> This creates a nice flow down the source code module from high level to low level.", "tr": "Yani çağrılan bir fonksiyon, onu çağıran fonksiyonun altında olmalıdır.<sup>2</sup> Bu da kaynak kod modülünde üst seviyeden alt seviyeye doğru güzel bir akış oluşturur.", "html": true}]}, {"type": "para", "sentences": [{"en": "As in newspaper articles, we expect the most important concepts to come first, and we expect them to be expressed with the least amount of polluting detail.", "tr": "Gazete makalelerinde olduğu gibi, en önemli kavramların önce gelmesini ve bunların olabildiğince az kirletici ayrıntıyla ifade edilmesini bekleriz."}, {"en": "We expect the low-level details to come last.", "tr": "Alt seviye ayrıntıların ise en sona gelmesini bekleriz."}, {"en": "This allows us to skim source files, getting the gist from the", "tr": "Bu sayede kaynak dosyalarına göz gezdirip ana fikri"}]}, {"type": "footnote", "en": "2. This is the exact opposite of languages like Pascal, C, and C++ that enforce functions to be defined, or at least declared, before they are used.", "tr": "2. Bu, fonksiyonların kullanılmadan önce gövdeleriyle birlikte yazılmasını ya da en azından tanımlanmasını (declared) zorunlu kılan Pascal, C ve C++ gibi dillerin tam tersidir."}], "concepts": [{"id": "conceptual-affinity-group-similar-functions", "title": {"en": "Conceptual Affinity: Keep Look-Alike Functions Together", "tr": "Kavramsal Yakınlık (Conceptual Affinity): Benzer Fonksiyonları Bir Arada Tutun"}, "summary": {"en": "Functions that share a naming scheme and do variations of the same job belong next to each other, even when none of them calls the others. Their closeness tells the reader \"these are one family\".", "tr": "Ortak bir isimlendirme şemasını paylaşan ve aynı işin çeşitlemelerini yapan fonksiyonlar, hiçbiri diğerini çağırmasa bile yan yana durmalıdır. Bu yakınlık okuyucuya \"bunlar tek bir aile\" mesajını verir."}, "bad": {"lang": "javascript", "code": "export function formatPrice(amount) {\n  return `${amount.toFixed(2)} TL`;\n}\n\nexport function parseQuantity(text) {\n  return Number.parseInt(text, 10);\n}\n\nexport function formatPercent(ratio) {\n  return `${(ratio * 100).toFixed(1)}%`;\n}\n\nexport function parseDate(isoDay) {\n  return new Date(`${isoDay}T00:00:00`);\n}\n\nexport function formatDate(date) {\n  return date.toLocaleDateString(\"tr-TR\");\n}", "why": {"en": "The format* and parse* helpers are interleaved. To see every formatter the reader must jump over unrelated parsing code, and it is easy to miss that formatDate exists at the bottom.", "tr": "format* ve parse* yardımcıları iç içe geçmiş. Okuyucu tüm biçimlendiricileri görmek için alakasız ayrıştırma kodunun üzerinden atlamak zorunda kalıyor; en altta bir formatDate olduğunu gözden kaçırmak da çok kolay."}}, "good": {"lang": "javascript", "code": "export function formatPrice(amount) {\n  return `${amount.toFixed(2)} TL`;\n}\n\nexport function formatPercent(ratio) {\n  return `${(ratio * 100).toFixed(1)}%`;\n}\n\nexport function formatDate(date) {\n  return date.toLocaleDateString(\"tr-TR\");\n}\n\n\nexport function parseQuantity(text) {\n  return Number.parseInt(text, 10);\n}\n\nexport function parseDate(isoDay) {\n  return new Date(`${isoDay}T00:00:00`);\n}", "why": {"en": "Each family sits in its own contiguous block. The format* group reads as one unit, and a new formatter obviously belongs right beside the others.", "tr": "Her aile kendi bitişik bloğunda duruyor. format* grubu tek bir birim olarak okunuyor ve yeni bir biçimlendiricinin nereye ekleneceği apaçık: diğerlerinin hemen yanına."}}, "tip": {"en": "When you add a function, ask \"which existing function is its closest sibling?\" and put it there, not at the end of the file.", "tr": "Yeni bir fonksiyon eklerken \"mevcut fonksiyonlardan hangisi bunun en yakın kardeşi?\" diye sorun ve onu dosyanın sonuna değil, o kardeşin yanına koyun."}}, {"id": "vertical-ordering-high-level-first", "title": {"en": "Vertical Ordering: High Level at the Top", "tr": "Dikey Sıralama (Vertical Ordering): Üst Seviye En Üstte"}, "summary": {"en": "Call dependencies should point downward: the public, high-level method comes first and the low-level helpers it calls follow below it. A reader can stop after the first method and still know what the class does.", "tr": "Çağrı bağımlılıkları aşağıyı göstermelidir: önce herkese açık, üst seviye metot gelir; onun çağırdığı alt seviye yardımcılar altında sıralanır. Okuyucu ilk metottan sonra okumayı bıraksa bile sınıfın ne yaptığını bilir."}, "bad": {"lang": "java", "code": "public class InvoiceReport {\n  private String formatLine(LineItem item) {\n    return item.name() + \": \" + item.total();\n  }\n\n  private Money sumTotals(List<LineItem> items) {\n    return items.stream()\n        .map(LineItem::total)\n        .reduce(Money.ZERO, Money::add);\n  }\n\n  private String renderLines(List<LineItem> items) {\n    return items.stream()\n        .map(this::formatLine)\n        .collect(Collectors.joining(\"\\n\"));\n  }\n\n  public String render(Invoice invoice) {\n    List<LineItem> items = invoice.items();\n    return renderLines(items) + \"\\nTotal: \" + sumTotals(items);\n  }\n}", "why": {"en": "The only public method, the one that explains the class, is buried at the bottom. The reader wades through formatting and summing details before learning what they are for.", "tr": "Sınıfı açıklayan tek public metot en altta gömülü kalmış. Okuyucu, bunların ne işe yaradığını öğrenmeden önce biçimlendirme ve toplama ayrıntılarının arasından geçmek zorunda."}}, "good": {"lang": "java", "code": "public class InvoiceReport {\n  public String render(Invoice invoice) {\n    List<LineItem> items = invoice.items();\n    return renderLines(items) + \"\\nTotal: \" + sumTotals(items);\n  }\n\n  private String renderLines(List<LineItem> items) {\n    return items.stream()\n        .map(this::formatLine)\n        .collect(Collectors.joining(\"\\n\"));\n  }\n\n  private String formatLine(LineItem item) {\n    return item.name() + \": \" + item.total();\n  }\n\n  private Money sumTotals(List<LineItem> items) {\n    return items.stream()\n        .map(LineItem::total)\n        .reduce(Money.ZERO, Money::add);\n  }\n}", "why": {"en": "render is the headline; each helper appears below the method that calls it. The file flows from \"what\" to \"how\", like a newspaper article.", "tr": "render manşet görevi görüyor; her yardımcı metot, onu çağıran metodun altında yer alıyor. Dosya bir gazete makalesi gibi \"ne\"den \"nasıl\"a doğru akıyor."}}, "tip": {"en": "Read your class from the top and stop after the first method. If you cannot tell what the class is for, the ordering is upside down.", "tr": "Sınıfınızı yukarıdan okumaya başlayın ve ilk metottan sonra durun. Sınıfın ne işe yaradığını anlayamıyorsanız sıralama baş aşağı demektir."}}, {"id": "define-before-use-habit", "title": {"en": "Don't Carry the \"Define Before Use\" Habit", "tr": "\"Kullanmadan Önce Tanımla\" Alışkanlığını Taşımayın"}, "summary": {"en": "Languages like C and Pascal force helpers above their callers, which produces bottom-up files. Java, Python and JavaScript do not require this, so you are free to put the entry point first and the details below.", "tr": "C ve Pascal gibi diller yardımcı fonksiyonları çağıranların üstüne koymaya zorlar; bu da aşağıdan yukarıya okunan dosyalar üretir. Java, Python ve JavaScript bunu şart koşmaz; dolayısıyla giriş noktasını öne, ayrıntıları aşağıya koymakta özgürsünüz."}, "bad": {"lang": "javascript", "code": "const toCents = (amount) => Math.round(amount * 100);\n\nconst applyDiscount = (cents, rate) => cents - Math.round(cents * rate);\n\nconst fromCents = (cents) => (cents / 100).toFixed(2);\n\nexport function checkoutTotal(cart) {\n  const cents = cart.items.reduce((sum, item) => sum + toCents(item.price), 0);\n  return fromCents(applyDiscount(cents, cart.discountRate));\n}", "why": {"en": "Out of a C-style reflex, every helper is written before checkoutTotal. The module opens with cents arithmetic, and the one exported function, the reason the file exists, comes last.", "tr": "C tarzı bir refleksle her yardımcı fonksiyon checkoutTotal'dan önce yazılmış. Modül kuruş hesabıyla açılıyor; dosyanın var olma sebebi olan tek dışa aktarılan fonksiyon ise en sona kalmış."}}, "good": {"lang": "javascript", "code": "export function checkoutTotal(cart) {\n  const cents = cart.items.reduce((sum, item) => sum + toCents(item.price), 0);\n  return fromCents(applyDiscount(cents, cart.discountRate));\n}\n\nfunction toCents(amount) {\n  return Math.round(amount * 100);\n}\n\nfunction applyDiscount(cents, rate) {\n  return cents - Math.round(cents * rate);\n}\n\nfunction fromCents(cents) {\n  return (cents / 100).toFixed(2);\n}", "why": {"en": "Function declarations are hoisted, so checkoutTotal can call helpers defined further down. The module now starts with its purpose and ends with its details.", "tr": "Fonksiyon bildirimleri yukarı taşındığı (hoisting) için checkoutTotal, daha aşağıda tanımlanan yardımcıları çağırabilir. Modül artık amacıyla başlıyor, ayrıntılarıyla bitiyor."}}, "tip": {"en": "In JavaScript, const arrow functions are not hoisted: they work below their caller only if the caller runs after the module has finished loading. Use function declarations when you want top-down ordering to be safe.", "tr": "JavaScript'te const ile tanımlanan ok fonksiyonları yukarı taşınmaz: çağıranın altında ancak çağıran fonksiyon modül yüklenmesi bittikten sonra çalışıyorsa sorunsuz çalışırlar. Yukarıdan aşağıya sıralamanın güvenli olmasını istiyorsanız fonksiyon bildirimlerini kullanın."}}]});
