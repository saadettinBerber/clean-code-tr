window.PAGE({"id": "page-92", "page": 92, "pdf_page": 92, "chapter": {"num": 5, "en": "Formatting", "tr": "Biçimlendirme"}, "section": {"en": "Uncle Bob's Formatting Rules", "tr": "Uncle Bob'un Biçimlendirme Kuralları"}, "title": {"en": "Uncle Bob's Formatting Rules (continued)", "tr": "Uncle Bob'un Biçimlendirme Kuralları (devam)"}, "blocks": [{"type": "caption", "kind": "listing", "en": "Listing 5-6 (continued) CodeAnalyzer.java", "tr": "Listing 5-6 (devamı) CodeAnalyzer.java"}, {"type": "code", "lang": "java", "code": "  public int getWidestLineNumber() {\n    return widestLineNumber;\n  }\n\n  public LineWidthHistogram getLineWidthHistogram() {\n    return lineWidthHistogram;\n  }\n\n  public double getMeanLineWidth() {\n    return (double)totalChars/lineCount;\n  }\n\n  public int getMedianLineWidth() {\n    Integer[] sortedWidths = getSortedWidths();\n    int cumulativeLineCount = 0;\n    for (int width : sortedWidths) {\n      cumulativeLineCount += lineCountForWidth(width);\n      if (cumulativeLineCount > lineCount/2)\n        return width;\n    }\n    throw new Error(\"Cannot get here\");\n  }\n\n  private int lineCountForWidth(int width) {\n    return lineWidthHistogram.getLinesforWidth(width).size();\n  }\n\n  private Integer[] getSortedWidths() {\n    Set<Integer> widths = lineWidthHistogram.getWidths();\n    Integer[] sortedWidths = (widths.toArray(new Integer[0]));\n    Arrays.sort(sortedWidths);\n    return sortedWidths;\n  }\n}"}], "concepts": [{"id": "vertical-ordering-dependent-functions", "title": {"en": "Vertical Ordering of Dependent Functions", "tr": "Bağımlı Fonksiyonların Dikey Sıralanması (Vertical Ordering)"}, "summary": {"en": "When a function calls another, the caller should sit just above the callee, and the source file should read top-to-bottom like a newspaper: general concepts first, details as you go down. Notice how getMedianLineWidth appears above the private helpers lineCountForWidth and getSortedWidths that it depends on — exactly the pattern this chapter recommends.", "tr": "Bir fonksiyon başka bir fonksiyonu çağırıyorsa, çağıran fonksiyon çağrılanın hemen üstünde yer almalı ve kaynak dosya bir gazete gibi yukarıdan aşağıya, genelden özele doğru okunmalıdır. getMedianLineWidth metodunun, bağımlı olduğu lineCountForWidth ve getSortedWidths yardımcı metotlarının hemen üstünde tanımlanmasına dikkat edin — bu tam olarak bu bölümün önerdiği düzendir."}, "bad": {"lang": "python", "code": "def helper_a():\n    return 42\n\ndef helper_b():\n    return helper_a() + 1\n\ndef calculate_total():\n    return helper_b() * 2", "why": {"en": "The reader meets helper_a and helper_b before knowing why they matter, then only discovers the real entry point, calculate_total, at the bottom — forcing an upward scroll to understand the flow.", "tr": "Okuyucu, helper_a ve helper_b fonksiyonlarının neden önemli olduğunu bilmeden onlarla karşılaşır; asıl giriş noktası olan calculate_total'ı ise ancak en altta görür. Bu da akışı anlamak için yukarı doğru kaydırma zorunluluğu doğurur."}}, "good": {"lang": "python", "code": "def calculate_total():\n    return helper_b() * 2\n\ndef helper_b():\n    return helper_a() + 1\n\ndef helper_a():\n    return 42", "why": {"en": "calculate_total is the concept the reader cares about first, so it comes first; each helper appears right below the function that depends on it, matching the natural top-down reading order.", "tr": "calculate_total, okuyucunun önce anlamak isteyeceği kavram olduğu için en üstte yer alır; her yardımcı fonksiyon, kendisine bağımlı olan fonksiyonun hemen altında görünür ve doğal yukarıdan aşağıya okuma sırasına uyar."}}, "tip": {"en": "Read your file from top to bottom as if it were an article: if you keep jumping backward to find a definition, your vertical ordering is fighting the reader.", "tr": "Dosyanızı sanki bir makaleymiş gibi baştan sona okuyun: bir tanımı bulmak için sürekli geriye zıplıyorsanız, dikey sıralamanız okuyucuya karşı çalışıyor demektir."}}, {"id": "small-focused-helper-methods", "title": {"en": "Small, Focused Private Helpers", "tr": "Küçük ve Odaklı Özel (Private) Yardımcı Metotlar"}, "summary": {"en": "getMedianLineWidth doesn't sort the widths or count lines itself — it delegates those single-purpose jobs to lineCountForWidth and getSortedWidths. Each private method does exactly one small thing, which keeps the public method readable at a glance.", "tr": "getMedianLineWidth genişlikleri kendisi sıralamaz ya da satırları kendisi saymaz — bu tek amaçlı işleri lineCountForWidth ve getSortedWidths metotlarına devreder (delegation). Her özel metot tam olarak tek bir küçük işi yapar; bu da genel (public) metodun tek bakışta okunabilir kalmasını sağlar."}, "bad": {"lang": "java", "code": "public int getMedianLineWidth() {\n  Set<Integer> widths = lineWidthHistogram.getWidths();\n  Integer[] sortedWidths = widths.toArray(new Integer[0]);\n  Arrays.sort(sortedWidths);\n  int cumulativeLineCount = 0;\n  for (int width : sortedWidths) {\n    cumulativeLineCount += lineWidthHistogram.getLinesforWidth(width).size();\n    if (cumulativeLineCount > lineCount / 2)\n      return width;\n  }\n  throw new Error(\"Cannot get here\");\n}", "why": {"en": "Sorting logic, counting logic, and the median search are all tangled into one method, so the reader has to parse every low-level detail just to see what the method is really trying to do.", "tr": "Sıralama mantığı, sayma mantığı ve medyan arama tek bir metodun içinde iç içe geçmiş; okuyucu metodun asıl ne yapmaya çalıştığını görebilmek için her düşük seviyeli detayı çözmek zorunda kalır."}}, "good": {"lang": "java", "code": "public int getMedianLineWidth() {\n  Integer[] sortedWidths = getSortedWidths();\n  int cumulativeLineCount = 0;\n  for (int width : sortedWidths) {\n    cumulativeLineCount += lineCountForWidth(width);\n    if (cumulativeLineCount > lineCount / 2)\n      return width;\n  }\n  throw new Error(\"Cannot get here\");\n}\n\nprivate int lineCountForWidth(int width) {\n  return lineWidthHistogram.getLinesforWidth(width).size();\n}\n\nprivate Integer[] getSortedWidths() {\n  Set<Integer> widths = lineWidthHistogram.getWidths();\n  Integer[] sortedWidths = widths.toArray(new Integer[0]);\n  Arrays.sort(sortedWidths);\n  return sortedWidths;\n}", "why": {"en": "Each helper has a name that states its single responsibility, so getMedianLineWidth reads almost like plain prose describing the median-finding algorithm.", "tr": "Her yardımcı metodun adı tek sorumluluğunu açıkça belirtir; bu sayede getMedianLineWidth, medyan bulma algoritmasını anlatan düz bir metin gibi okunur."}}, "tip": {"en": "If you need a comment to explain what a chunk of a method does, that chunk is probably a good candidate for its own well-named private method.", "tr": "Bir metodun bir bölümünü açıklamak için yoruma ihtiyaç duyuyorsanız, o bölüm muhtemelen kendi başına iyi isimlendirilmiş bir özel metoda dönüştürülmeye adaydır."}}, {"id": "descriptive-accessor-names", "title": {"en": "Descriptive Accessor Names", "tr": "Betimleyici Erişimci (Accessor) İsimleri"}, "summary": {"en": "getWidestLineNumber, getMeanLineWidth, and getMedianLineWidth each tell you precisely what statistic they return, without needing to read the implementation. A vague getValue or getStat would force every caller to go dig through the method body.", "tr": "getWidestLineNumber, getMeanLineWidth ve getMedianLineWidth, uygulamayı okumaya gerek kalmadan hangi istatistiği döndürdüklerini tam olarak söyler. Belirsiz bir getValue ya da getStat ismi ise her çağıran kodu metodun gövdesini kurcalamaya zorlardı."}, "bad": {"lang": "javascript", "code": "class LineStats {\n  getValue1() {\n    return this.totalChars / this.lineCount;\n  }\n\n  getValue2() {\n    return this.widestLineNumber;\n  }\n}", "why": {"en": "getValue1 and getValue2 give no clue about what they compute; every caller has to open the class and read the body just to know which one returns the average.", "tr": "getValue1 ve getValue2, ne hesapladıkları hakkında hiçbir ipucu vermez; her çağıran kod, hangisinin ortalamayı döndürdüğünü öğrenmek için sınıfı açıp gövdeyi okumak zorunda kalır."}}, "good": {"lang": "javascript", "code": "class LineStats {\n  getMeanLineWidth() {\n    return this.totalChars / this.lineCount;\n  }\n\n  getWidestLineNumber() {\n    return this.widestLineNumber;\n  }\n}", "why": {"en": "The names alone answer the question \"what does this return?\", so a caller can use the class correctly without ever opening its source.", "tr": "İsimlerin kendisi \"bu ne döndürür?\" sorusunu yanıtlar; böylece çağıran kod, sınıfın kaynağını hiç açmadan onu doğru şekilde kullanabilir."}}, "tip": {"en": "Name a getter after the value it returns, not after its implementation — getMeanLineWidth, not getCalculatedAverage.", "tr": "Bir erişimciyi (getter) uygulamasına göre değil, döndürdüğü değere göre adlandırın — getCalculatedAverage değil, getMeanLineWidth gibi."}}]});
