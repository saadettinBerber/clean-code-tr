window.PAGE({"id": "page-148", "page": 148, "pdf_page": 148, "chapter": {"num": 10, "en": "Classes", "tr": "Sınıflar"}, "section": {"en": "Organizing for Change", "tr": "Değişim İçin Düzenleme"}, "title": {"en": "A set of closed classes", "tr": "Kapalı sınıflar kümesi"}, "blocks": [{"type": "para", "sentences": [{"en": "they are needed. The common private behavior is isolated to a pair of utility classes, `Where` and `ColumnList`.", "tr": "ihtiyaç duyuldukları yere taşınır. Ortak private davranış ise `Where` ve `ColumnList` adlı bir çift yardımcı sınıfta (utility class) yalıtılmıştır."}]}, {"type": "caption", "kind": "listing", "en": "Listing 10-10 A set of closed classes", "tr": "Listing 10-10 Kapalı sınıflardan oluşan bir küme"}, {"type": "code", "lang": "java", "code": "abstract public class Sql {\n   public Sql(String table, Column[] columns)\n   abstract public String generate();\n}\n\npublic class CreateSql extends Sql {\n   public CreateSql(String table, Column[] columns)\n   @Override public String generate()\n}\n\npublic class SelectSql extends Sql {\n   public SelectSql(String table, Column[] columns)\n   @Override public String generate()\n}\n\npublic class InsertSql extends Sql {\n   public InsertSql(String table, Column[] columns, Object[] fields)\n   @Override public String generate()\n   private String valuesList(Object[] fields, final Column[] columns)\n}\n\npublic class SelectWithCriteriaSql extends Sql {\n   public SelectWithCriteriaSql(\n      String table, Column[] columns, Criteria criteria)\n   @Override public String generate()\n}\n\npublic class SelectWithMatchSql extends Sql {\n   public SelectWithMatchSql(\n      String table, Column[] columns, Column column, String pattern)\n   @Override public String generate()\n}\n\npublic class FindByKeySql extends Sql\n   public FindByKeySql(\n      String table, Column[] columns, String keyColumn, String keyValue)\n   @Override public String generate()\n}\n\npublic class PreparedInsertSql extends Sql {\n   public PreparedInsertSql(String table, Column[] columns)\n   @Override public String generate() {\n   private String placeholderList(Column[] columns)\n}\n\npublic class Where {\n   public Where(String criteria)\n   public String generate()\n}"}], "concepts": [{"id": "closed-class-hierarchy", "title": {"en": "One Closed Class per Operation", "tr": "Her İşlem İçin Kapalı Bir Sınıf"}, "summary": {"en": "Instead of one class that grows a new public method for every operation, give each operation its own small subclass of an abstract base. Existing classes stay closed to change.", "tr": "Her yeni işlem için yeni bir public metot büyüten tek bir sınıf yerine, her işleme soyut bir taban sınıfın kendi küçük alt sınıfını verin. Mevcut sınıflar değişikliğe kapalı kalır."}, "bad": {"lang": "java", "code": "public class ReportExporter {\n   public String toPdf(Report r) { ... }\n   public String toCsv(Report r) { ... }\n   public String toHtml(Report r) { ... }\n   // adding toXml() means editing this class again\n   private String header(Report r) { ... }\n   private String escapeHtml(String s) { ... }\n}", "why": {"en": "Every new format reopens `ReportExporter`. Private helpers that serve only one format still live in the shared class, so a change for HTML can break PDF.", "tr": "Her yeni biçim `ReportExporter` sınıfını yeniden açar. Yalnızca tek bir biçime hizmet eden private yardımcılar yine ortak sınıfta yaşar; HTML için yapılan bir değişiklik PDF'i bozabilir."}}, "good": {"lang": "java", "code": "abstract public class ReportExport {\n   public ReportExport(Report report)\n   abstract public String generate();\n}\n\npublic class PdfExport extends ReportExport {\n   @Override public String generate()\n}\n\npublic class CsvExport extends ReportExport {\n   @Override public String generate()\n}\n\npublic class HtmlExport extends ReportExport {\n   @Override public String generate()\n   private String escapeHtml(String s)\n}", "why": {"en": "Adding XML means writing `XmlExport`; nothing else is touched. `escapeHtml` moved into the only class that needs it.", "tr": "XML eklemek `XmlExport` yazmak demektir; başka hiçbir şeye dokunulmaz. `escapeHtml` yalnızca ona ihtiyaç duyan sınıfa taşındı."}}, "tip": {"en": "When you catch yourself adding a public method to a class every time a requirement arrives, ask whether each method should be its own class.", "tr": "Her gereksinim geldiğinde bir sınıfa public metot eklediğinizi fark ederseniz, her metodun aslında kendi sınıfı olması gerekip gerekmediğini sorun."}}, {"id": "shared-private-behavior-utility", "title": {"en": "Isolate Shared Behavior in Utility Classes", "tr": "Ortak Davranışı Yardımcı Sınıflarda Yalıtın"}, "summary": {"en": "When several subclasses need the same private helper, do not copy it or push it into the base class; extract it into a small, focused utility class they all use.", "tr": "Birden çok alt sınıf aynı private yardımcıya ihtiyaç duyuyorsa onu kopyalamayın ya da taban sınıfa itmeyin; hepsinin kullandığı küçük, odaklı bir yardımcı sınıfa (utility class) çıkarın."}, "bad": {"lang": "python", "code": "class InvoicePrinter:\n    def render(self, invoice):\n        total = f\"{invoice.total / 100:,.2f} {invoice.currency}\"\n        ...\n\nclass ReceiptPrinter:\n    def render(self, receipt):\n        total = f\"{receipt.total / 100:,.2f} {receipt.currency}\"\n        ...", "why": {"en": "The money-formatting rule is duplicated. When the format changes (say, thousands separators), you must find and fix every copy.", "tr": "Para biçimlendirme kuralı tekrarlanmış. Biçim değiştiğinde (örneğin binlik ayırıcılar) her kopyayı bulup düzeltmeniz gerekir."}}, "good": {"lang": "python", "code": "class MoneyFormatter:\n    def __init__(self, currency):\n        self.currency = currency\n\n    def format(self, cents):\n        return f\"{cents / 100:,.2f} {self.currency}\"\n\n\nclass InvoicePrinter:\n    def render(self, invoice):\n        total = MoneyFormatter(invoice.currency).format(invoice.total)\n        ...\n\n\nclass ReceiptPrinter:\n    def render(self, receipt):\n        total = MoneyFormatter(receipt.currency).format(receipt.total)\n        ...", "why": {"en": "`MoneyFormatter` has one reason to change and can be tested on its own. Both printers stay small and ignorant of formatting details.", "tr": "`MoneyFormatter` sınıfının değişmek için tek bir nedeni var ve tek başına test edilebilir. İki yazıcı da küçük kalır ve biçimlendirme ayrıntılarından habersizdir."}}, "tip": {"en": "A utility class like `Where` or `ColumnList` is not a dumping ground; it should have a name that tells you exactly what it produces.", "tr": "`Where` ya da `ColumnList` gibi bir yardımcı sınıf çöplük değildir; adı tam olarak ne ürettiğini söylemelidir."}}, {"id": "extend-by-adding-subclass", "title": {"en": "Extend by Adding, Not Editing", "tr": "Düzenleyerek Değil, Ekleyerek Genişletin"}, "summary": {"en": "A design that follows the Open-Closed Principle lets you add a new capability by adding a new class, without editing code that already works.", "tr": "Açık Kapalı İlkesi'ne (OCP) uyan bir tasarım, halihazırda çalışan koda dokunmadan yeni bir sınıf ekleyerek yeni bir yetenek kazandırmanıza izin verir."}, "bad": {"lang": "javascript", "code": "function notify(channel, user, message) {\n  switch (channel) {\n    case 'email': return sendEmail(user.email, message);\n    case 'sms':   return sendSms(user.phone, message);\n    // every new channel: edit this switch and retest all of it\n  }\n}", "why": {"en": "Adding push notifications forces you to modify `notify`, and any mistake there can break email and SMS at the same time.", "tr": "Anlık bildirim (push) eklemek sizi `notify` fonksiyonunu değiştirmeye zorlar; oradaki bir hata e-posta ile SMS'i aynı anda bozabilir."}}, "good": {"lang": "javascript", "code": "class Notifier {\n  send(user, message) { throw new Error('override me'); }\n}\n\nclass EmailNotifier extends Notifier {\n  send(user, message) { return sendEmail(user.email, message); }\n}\n\nclass SmsNotifier extends Notifier {\n  send(user, message) { return sendSms(user.phone, message); }\n}\n\n// New requirement: only a new file, nothing else changes\nclass PushNotifier extends Notifier {\n  send(user, message) { return sendPush(user.deviceToken, message); }\n}", "why": {"en": "`PushNotifier` is a pure addition. The existing notifiers and their tests are untouched, so the risk of the change is confined to the new class.", "tr": "`PushNotifier` saf bir eklemedir. Mevcut bildiriciler ve testleri olduğu gibi kalır; değişikliğin riski yeni sınıfla sınırlıdır."}}, "tip": {"en": "Before touching a working class to add a feature, ask: could this be a new subclass instead?", "tr": "Bir özellik eklemek için çalışan bir sınıfa dokunmadan önce sorun: Bu, bunun yerine yeni bir alt sınıf olabilir mi?"}}]});
