window.PAGE({"id": "page-234", "page": 234, "pdf_page": 234, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Adding set to ArgumentMarshaler", "tr": "ArgumentMarshaler'a set Metodu Ekleniyor"}, "blocks": [{"type": "para", "sentences": [{"en": "So now `setBooleanArg` is useless.", "tr": "Böylece `setBooleanArg` artık işe yaramaz."}, {"en": "If there were a set function in `ArgumentMarshaler`, we could call it directly.", "tr": "`ArgumentMarshaler`'da bir set fonksiyonu olsaydı, onu doğrudan çağırabilirdik."}, {"en": "So it’s time to make that function!", "tr": "O halde o fonksiyonu yazma zamanı!"}, {"en": "The first step is to add the new abstract method to `ArgumentMarshaler`.", "tr": "İlk adım, yeni soyut metodu `ArgumentMarshaler`'a eklemek."}]}, {"type": "code", "lang": "java", "code": "  private abstract class ArgumentMarshaler {\n    public abstract void set(Iterator<String> currentArgument)\n                         throws ArgsException;\n    public abstract void set(String s) throws ArgsException;\n    public abstract Object get();\n  }"}, {"type": "para", "sentences": [{"en": "Of course this breaks all the derivatives.", "tr": "Tabii ki bu tüm türevleri (derivatives) bozuyor."}, {"en": "So let’s implement the new method in each.", "tr": "O halde yeni metodu her birinde gerçekleştirelim."}]}, {"type": "code", "lang": "java", "code": "  private class BooleanArgumentMarshaler extends ArgumentMarshaler {\n    private boolean booleanValue = false;\n\n    public void set(Iterator<String> currentArgument) throws ArgsException {\n     booleanValue = true;\n    }\n\n    public void set(String s) {\n      booleanValue = true;\n    }\n\n    public Object get() {\n      return booleanValue;\n    }\n  }\n\n  private class StringArgumentMarshaler extends ArgumentMarshaler {\n    private String stringValue = \"\";\n\n    public void set(Iterator<String> currentArgument) throws ArgsException {\n    }\n\n    public void set(String s) {\n      stringValue = s;\n    }\n\n    public Object get() {\n      return stringValue;\n    }\n  }\n\n  private class IntegerArgumentMarshaler extends ArgumentMarshaler {\n    private int intValue = 0;\n\n    public void set(Iterator<String> currentArgument) throws ArgsException {\n    }\n\n    public void set(String s) throws ArgsException {\n      try {\n        intValue = Integer.parseInt(s);\n      } catch (NumberFormatException e) {\n        throw new ArgsException();\n      }\n    }"}], "concepts": [{"id": "abstract-method-migration-hook", "title": {"en": "Abstract Method as a Migration Hook", "tr": "Göç Kancası Olarak Soyut Metot (Abstract Method)"}, "summary": {"en": "When you add an abstract method to a base class, the compiler forces every derivative to implement it before the code compiles. There is no default, no no-op body, no subclass left behind.", "tr": "Bir üst sınıfa soyut (abstract) metot eklediğinizde derleyici, kod derlenmeden önce her türevin onu gerçekleştirmesini zorunlu kılar. Varsayılan gövde yok, boş (no-op) gövde yok, geride kalan alt sınıf yok."}, "bad": {"lang": "java", "code": "class ArgumentMarshaler {\n  // empty default so derivatives can \"ignore it for now\"\n  public void set(Iterator<String> currentArgument)\n      throws ArgsException {\n  }\n}", "why": {"en": "With an empty default body, any derivative that forgets to implement set() compiles fine and silently drops the behavior — the compiler cannot tell you the migration is incomplete.", "tr": "Boş varsayılan gövdeyle set() gerçekleştirmeyi unutan her türev rahatça derlenir ve davranışı sessizce kaybeder — derleyici göçün yarımda kaldığını söyleyemez."}}, "good": {"lang": "java", "code": "abstract class ArgumentMarshaler {\n  public abstract void set(Iterator<String> currentArgument)\n      throws ArgsException;\n}\n\nclass BooleanArgumentMarshaler extends ArgumentMarshaler {\n  @Override\n  public void set(Iterator<String> currentArgument)\n      throws ArgsException {\n    booleanValue = true;\n  }\n}", "why": {"en": "Marking set() abstract makes the compiler reject any derivative that does not implement it — a half-done migration simply does not compile, so it cannot ship.", "tr": "set() metodu soyut (abstract) işaretlenince, onu gerçekleştirmeyen her türevi derleyici reddeder — yarıda kalmış bir göç derlenemez, dolayısıyla devreye alınamaz."}}, "tip": {"en": "When you introduce a new contract in a base class, prefer abstract over an empty default: the compiler becomes your migration checklist.", "tr": "Bir üst sınıfa yeni bir sözleşme getirdiğinizde boş varsayılan yerine soyut (abstract) metodu tercih edin: derleyici sizin göç kontrol listeniz olsun."}}, {"id": "delete-the-useless", "title": {"en": "Delete the Useless", "tr": "İşe Yaransızı Sil (Delete the Useless)"}, "summary": {"en": "Once a method loses its last caller, delete it in the same step of the refactor. 'Let's keep it just in case' methods accumulate into dead code that makes readers wonder whether they still matter.", "tr": "Bir metot son çağrıcısını kaybettiği anda, yeniden düzenlemenin (refactoring) aynı adımında silin. 'Yoksa lazım olur' diye tutulan metotlar, okuyucunun hâlâ geçerli mi diye şaştığı ölü koda birikir."}, "bad": {"lang": "java", "code": "class Args {\n  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m == null) return false;\n    m.set(currentArgument);\n    return true;\n  }\n\n  // \"we might need this again later\"\n  private boolean setBooleanArg(ArgumentMarshaler m)\n      throws ArgsException {\n    m.set(currentArgument);\n    return true;\n  }\n}", "why": {"en": "setBooleanArg no longer has any caller after set() is deployed, yet it stays in the file; readers see the duplicate and assume the boolean path is still live.", "tr": "set() dağıtıldıktan sonra setBooleanArg'ın hiçbir çağrıcısı kalmamıştır, ama dosyada kalır; okuyucu kopyayı görüp boolean yolunun hâlâ geçerli olduğunu sanır."}}, "good": {"lang": "java", "code": "class Args {\n  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m == null) return false;\n    m.set(currentArgument);\n    return true;\n  }\n  // setBooleanArg deleted — the abstract set() covers booleans too\n}", "why": {"en": "The moment the last caller is replaced, the redundant method is deleted; version control keeps the old code recoverable, so 'just in case' has no reason to exist.", "tr": "Son çağrıcı yeni yola taşındığı anda gereksiz metot silinir; sürüm kontrolü eski kodu geri kazanılabilir tuttuğu için 'yoksa lazım olur'un var olma sebebi kalmaz."}}, "tip": {"en": "Before finishing a refactor, search the whole codebase for each method you made redundant: if it has no callers, delete it in the same commit.", "tr": "Bir yeniden düzenlemeyi (refactoring) bitirmeden, artık gereksiz kıldığı her metodu tüm kod tabanında arayın: çağrıcısı yoksa aynı commit'te silin."}}]});
