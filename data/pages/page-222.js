window.PAGE({"id": "page-222", "page": 222, "pdf_page": 222, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Eliminating Duplicate marshalers.get Calls", "tr": "Yinelenen marshalers.get Çağrılarını Giderme"}, "blocks": [{"type": "para", "sentences": [{"en": "The tests still passed. So I eliminated all the duplicate calls to `marshalers.get` as follows:", "tr": "Testler hâlâ geçiyordu. Böylece `marshalers.get`'e yinelenen tüm çağrıları şöyle ortadan kaldırdım:"}]}, {"type": "code", "lang": "java", "code": "  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (isBooleanArg(m))\n      setBooleanArg(argChar);\n    else if (isStringArg(m))\n      setStringArg(argChar);\n    else if (isIntArg(m))\n      setIntArg(argChar);\n    else\n      return false;\n\n    return true;\n  }\n\n  private boolean isIntArg(ArgumentMarshaler m) {\n    return m instanceof IntegerArgumentMarshaler;\n  }\n\n  private boolean isStringArg(ArgumentMarshaler m) {\n    return m instanceof StringArgumentMarshaler;\n  }\n\n  private boolean isBooleanArg(ArgumentMarshaler m) {\n    return m instanceof BooleanArgumentMarshaler;\n  }"}, {"type": "para", "sentences": [{"en": "This left no good reason for the three `isxxxArg` methods.", "tr": "Bu, üç `isxxxArg` metodu için iyi bir gerekçe bırakmadı."}, {"en": "So I inlined them:", "tr": "Böylece onları satır içine (inline) aldım:"}]}, {"type": "code", "lang": "java", "code": "  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m instanceof BooleanArgumentMarshaler)\n      setBooleanArg(argChar);\n    else if (m instanceof StringArgumentMarshaler)\n      setStringArg(argChar);\n    else if (m instanceof IntegerArgumentMarshaler)\n      setIntArg(argChar);\n    else\n      return false;\n\n    return true;\n  }"}, {"type": "para", "sentences": [{"en": "Next, I started using the marshalers map in the set functions, breaking the use of the other three maps.", "tr": "Sonra, set fonksiyonlarında marshalers haritasını kullanmaya başladım; böylece diğer üç haritanın kullanımını kırdım."}, {"en": "I started with the booleans.", "tr": "Önce booleanlerden başladım."}]}, {"type": "code", "lang": "java", "code": "  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m instanceof BooleanArgumentMarshaler)\n      setBooleanArg(m);\n    else if (m instanceof StringArgumentMarshaler)\n      setStringArg(argChar);\n    else if (m instanceof IntegerArgumentMarshaler)\n      setIntArg(argChar);\n    else\n      return false;"}], "concepts": [{"id": "inline-trivial-methods", "kind": "code", "title": {"en": "Inline Trivial Methods", "tr": "Önemsiz Metotları Satır İçi Alma (Inlining)"}, "summary": {"en": "A method that merely wraps a single expression and is called from one place adds indirection without value. Inlining it removes the cognitive break: the code reads as one statement instead of a name that must be jumped to.", "tr": "Tek bir ifadenin etrafını saran ve tek bir yerden çağrılan metot, değeri olmayan bir dolaylılık (indirection) ekler. Onu satır içine almak (inline) bilişsel kesintiyi (cognitive break) ortadan kaldırır: kod, içine sıçranması gereken bir isim yerine tek bir ifade gibi okunur."}, "bad": {"lang": "java", "code": "private boolean isIntArg(char argChar) {\n  return integerArgs.containsKey(argChar);\n}\n\n// ...\nif (isIntArg(argChar))\n  setIntArg(argChar);", "why": {"en": "The method name tells you almost nothing the expression does not already say; you must jump to the method body to learn that it is just a containsKey check on integerArgs.", "tr": "Metot adı, ifadenin kendisi zaten söylemeyeni neredeyse hiçbir şey söylemez; bunun integerArgs üzerinde sıradan bir containsKey denetimi olduğunu öğrenmek için metot gövdesine sıçramak zorunda kalırsınız."}}, "good": {"lang": "java", "code": "if (integerArgs.containsKey(argChar))\n  setIntArg(argChar);", "why": {"en": "The condition reads exactly as the code that performs it; there is no name to memorize and no jump to make.", "tr": "Koşul, onu gerçekleştiren kodun kendisi olarak okunur; ezilecek bir isim ve atılması gereken bir sıçrama yoktur."}}, "tip": {"en": "Before extracting a method, ask whether its name says something the body does not. If the method is trivial and has a single call site, leave it inlined and revisit the question only when more call sites appear.", "tr": "Bir metodu çıkarmadan önce, adının gövdesinin söylemediği bir şey söylüyor mu diye sorun. Metot önemsiz ve tek çağrı noktasına sahipse onu satır içi bırakın; yeni çağrı noktaları ortaya çıkınca soruyu yeniden değerlendirin."}}, {"id": "local-cache-map-lookup", "kind": "code", "title": {"en": "Local Cache of Map Lookup", "tr": "Harita Aramasının Yerel Önbelleği (Local Cache)"}, "summary": {"en": "When the same map lookup is needed in several branches, query the map once, store the result in a local variable, and let that variable carry the value through all the branches — instead of re-querying the map for every check.", "tr": "Aynı harita araması (map lookup) birden çok dalda gerekiyorsa, haritayı tek bir kez sorgulayın, sonucu yerel bir değişkende saklayın ve o değişkenin değeri tüm dallara taşımaya bırakın — her denetim için haritayı yeniden sorgulamak yerine."}, "bad": {"lang": "java", "code": "private boolean isBooleanArg(char argChar) {\n  return marshalers.get(argChar)\n      instanceof BooleanArgumentMarshaler;\n}\n\nif (isBooleanArg(argChar))\n  setBooleanArg(argChar);\nelse if (isStringArg(argChar))\n  setStringArg(argChar);\nelse if (isIntArg(argChar))\n  setIntArg(argChar);", "why": {"en": "Each is-xxx helper runs marshalers.get(argChar) again; for a single character the map is walked up to three times, and every check adds a call boundary between the reader and the data.", "tr": "Her is-xxx yardımcı metodu marshalers.get(argChar) ifadesini yeniden çalıştırır; tek bir karakter için harita en çok üç kez aranır ve her denetim, okuyucu ile veri arasına bir çağrı sınırı ekler."}}, "good": {"lang": "java", "code": "ArgumentMarshaler m = marshalers.get(argChar);\nif (m instanceof BooleanArgumentMarshaler)\n  setBooleanArg(argChar);\nelse if (m instanceof StringArgumentMarshaler)\n  setStringArg(argChar);\nelse if (m instanceof IntegerArgumentMarshaler)\n  setIntArg(argChar);", "why": {"en": "The map is consulted exactly once; the local variable m then carries the result through every branch, and the code reads as one continuous decision.", "tr": "Harita tam bir kez sorgulanır; yerel değişken m sonucu tüm dallara taşır ve kod, kesintisiz tek bir karar gibi okunur."}}, "tip": {"en": "This is caching by scope, not caching by design: one lookup per decision, stored exactly where it is used. Apply the same trick to any repeated expression — it makes intent clearer and the code cheaper, at zero cost.", "tr": "Bu, tasarımdan değil kapsamdan önbelleklemedir (caching): her karar için tek arama, kullanıldığı yerde saklanmış. Aynı hileni her yinelenen ifadeye uygulayabilirsiniz — niyeti daha açık, kodu daha ucuz kılar; maliyeti sıfırdır."}}]});
