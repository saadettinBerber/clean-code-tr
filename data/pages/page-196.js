window.PAGE({"id": "page-196", "page": 196, "pdf_page": 196, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "Args Implementation", "tr": "Args Gerçekleştirimi"}, "title": {"en": "Args.java — Listing 14-2 (continued)", "tr": "Args.java — Listing 14-2 (devamı)"}, "blocks": [{"type": "code", "lang": "java", "code": "  private void parseArgumentCharacters(String argChars) throws ArgsException {\n    for (int i = 0; i < argChars.length(); i++)\n      parseArgumentCharacter(argChars.charAt(i));\n  }\n\n  private void parseArgumentCharacter(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m == null) {\n      throw new ArgsException(UNEXPECTED_ARGUMENT, argChar, null);\n    } else {\n      argsFound.add(argChar);\n      try {\n        m.set(currentArgument);\n      } catch (ArgsException e) {\n        e.setErrorArgumentId(argChar);\n        throw e;\n      }\n    }\n  }\n\n  public boolean has(char arg) {\n    return argsFound.contains(arg);\n  }\n\n  public int nextArgument() {\n    return currentArgument.nextIndex();\n  }\n\n  public boolean getBoolean(char arg) {\n    return BooleanArgumentMarshaler.getValue(marshalers.get(arg));\n  }\n\n  public String getString(char arg) {\n    return StringArgumentMarshaler.getValue(marshalers.get(arg));\n  }\n\n  public int getInt(char arg) {\n    return IntegerArgumentMarshaler.getValue(marshalers.get(arg));\n  }\n\n  public double getDouble(char arg) {\n    return DoubleArgumentMarshaler.getValue(marshalers.get(arg));\n  }\n\n  public String[] getStringArray(char arg) {\n    return StringArrayArgumentMarshaler.getValue(marshalers.get(arg));\n  }\n}"}, {"type": "caption", "en": "Notice that you can read this code from the top to the bottom without a lot of jumping around or looking ahead. The one thing you may have had to look ahead for is the definition of `ArgumentMarshaler`, which I left out intentionally. Having read this code carefully,", "tr": "Bu kodu üstten alta, çok fazla buraya oraya zıplamadan ya da ileriye bakmadan okuyabilirsin. İleriye bakman gerektiği tek yer `ArgumentMarshaler` tanımı olabilir ki onu ben bilerek dışarıda bıraktım. Bu kodu dikkatle okumuş olduğun için,"}], "concepts": [{"id": "deferred-definition", "kind": "code", "title": {"en": "Deferred Definition", "tr": "Ertelemeli Tanım (Deferred Definition)"}, "summary": {"en": "When a listing introduces several components, let the reader follow the main flow first and meet each supporting component right when the flow first needs it. Defining every dependency up front turns a narrative into an inventory.", "tr": "Bir listing birden çok bileşen tanıtıyorsa, okuyucuya önce ana akışı izlet ve her yardımcı bileşeni akışın ona gerçekten ihtiyaç duyduğu ilk noktada tanıt. Tüm bağımlılıkları baştan tanımlamak, anlatıyı envantere çevirir."}, "bad": {"lang": "python", "code": "# Bad: the whole cast is introduced before the story starts\nclass RedisCache:\n    def __init__(self, url):\n        self.url = url\n\n    def get(self, key):\n        ...\n\nclass SmtpMailer:\n    def __init__(self, host):\n        self.host = host\n\n    def send(self, msg):\n        ...\n\nclass ReportPrinter:\n    def __init__(self):\n        self.cache = RedisCache(\"redis://localhost:6379\")\n        self.mailer = SmtpMailer(\"smtp.example.com\")\n\n    def run(self):\n        row = self.cache.get(\"report-142\")\n        self.mailer.send(row.summary)", "why": {"en": "The reader must absorb two helper classes before reaching the one that was the point of the listing. Every definition dumped up front adds to the mental burden and delays the main flow.", "tr": "Okuyucu, okumaya gelilen sınıfa ulaşmadan önce iki yardımcı sınıfı sindirmek zorunda kalır. Başa yığılan her tanım zihinsel yükü (mental burden) artırır ve ana akışı geciktirir."}}, "good": {"lang": "python", "code": "# Good: the main flow first; helpers are defined later, at first need\nclass ReportPrinter:\n    def __init__(self):\n        self.cache = RedisCache(\"redis://localhost:6379\")  # aşağıda\n        self.mailer = SmtpMailer(\"smtp.example.com\")       # aşağıda\n\n    def run(self):\n        row = self.cache.get(\"report-142\")\n        self.mailer.send(row.summary)\n\n# --- supporting cast, only after the flow is clear ---\nclass RedisCache:\n    def get(self, key):\n        ...\n\nclass SmtpMailer:\n    def send(self, msg):\n        ...", "why": {"en": "The reader gets the story of ReportPrinter in six lines. The helper classes arrive exactly when the reader wants to know how they work — the same order in which the information becomes useful.", "tr": "Okuyucu, ReportPrinter'ın hikâyesini altı satırda alır. Yardımcı sınıflar, okuyucunun gerçekten nasıl çalıştığını merak ettiği anda devreye girer — bilginin işe yaradığı sırayla birebir aynı sıra."}}, "tip": {"en": "Like a newspaper: run the headline and the lede first, and leave the profiles and appendices for after the story has been told.", "tr": "Gazete gibi: önce manşeti ve giriş paragrafını yürüt; portreleri ve ekleri hikâye anlatıldıktan sonraya bırak."}}, {"id": "linear-code-narrative", "kind": "code", "title": {"en": "Linear Code Narrative", "tr": "Doğrusal Kod Anlatısı (Linear Code Narrative)"}, "summary": {"en": "A source file should read like prose, top to bottom. Every method should sit as close to its first call as possible, so the reader never has to scroll back up or search ahead.", "tr": "Bir kaynak dosya, üstten alta düzyazı gibi okunmalıdır. Her metot, ilk çağrısına olabildiğince yakın durmalıdır; böylece okuyucu ne geri kaydırır ne de ileriye arama yapar."}, "bad": {"lang": "js", "code": "// Bad: first use is 60 lines above its definition\nfunction processOrder(order) {\n  const checked = validateOrder(order);\n  const total = applyLoyalty(checked, findCustomer(order.id));\n  return ship(total);\n}\n\n// ... 60 lines of unrelated logging and config helpers ...\n\nfunction validateOrder(order) { /* ... */ }\nfunction findCustomer(id) { /* ... */ }", "why": {"en": "To understand one small function, the reader must jump back and forth through the file. Every jump is a cognitive break that shatters the flow.", "tr": "Küçük bir fonksiyonu anlamak için okuyucu dosya içinde ileri-geri zıplamak zorunda kalır. Her zıplama, akışı kesen bir bilişsel kesinti (cognitive break) yaratır."}}, "good": {"lang": "js", "code": "// Good: each helper sits right below its first call\nfunction processOrder(order) {\n  const checked = validateOrder(order);\n  const total = applyLoyalty(checked, findCustomer(order.id));\n  return ship(total);\n}\n\nfunction validateOrder(order) { /* ... */ }\n\nfunction findCustomer(id) { /* ... */ }", "why": {"en": "The reader can now walk from the top to the bottom in a single pass: every name is defined just below where it was first met. No backtracking, no searching.", "tr": "Okuyucu artık üstten alta tek geçişte yürüyebilir: her isim, ilk karşılaşıldığı yerin hemen altında tanımlanır. Geriye dönüş yok, arama yok."}}, "tip": {"en": "Test your file: read it top to bottom; every time you have to scroll up to understand a line, move that definition next to its first use.", "tr": "Dosyanı test et: üstten alta oku; bir satırı anlamak için her yukarı kaydırmada, o tanımı ilk kullanımının yanına taşı."}}]});
