window.PAGE({"id": "page-240", "page": 240, "pdf_page": 240, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Refactored Args.java (continued)", "tr": "Yeniden Düzenlenmiş Args.java (devamı)"}, "blocks": [{"type": "code", "lang": "java", "code": "public class Args {\n  ...\n  private char errorArgumentId = '\\0';\n  private String errorParameter = \"TILT\";\n  private ArgsException.ErrorCode errorCode = ArgsException.ErrorCode.OK;\n  private List<String> argsList;\n\n  public Args(String schema, String[] args) throws ArgsException {\n    this.schema = schema;\n    argsList = Arrays.asList(args);\n    valid = parse();\n  }\n\n  private boolean parse() throws ArgsException {\n    if (schema.length() == 0 && argsList.size() == 0)\n      return true;\n    parseSchema();\n    try {\n      parseArguments();\n    } catch (ArgsException e) {\n    }\n    return valid;\n  }\n\n  private boolean parseSchema() throws ArgsException {\n    ...\n  }\n\n  private void parseSchemaElement(String element) throws ArgsException {\n    ...\n    else\n      throw new ArgsException(\n        String.format(\"Argument: %c has invalid format: %s.\",\n                      elementId,elementTail));\n  }\n\n  private void validateSchemaElementId(char elementId) throws ArgsException {\n    if (!Character.isLetter(elementId)) {\n      throw new ArgsException(\n        \"Bad character:\" + elementId + \"in Args format: \" + schema);\n    }\n  }\n\n  ...\n\n  private void parseElement(char argChar) throws ArgsException {\n    if (setArgument(argChar))\n      argsFound.add(argChar);\n    else {\n      unexpectedArguments.add(argChar);\n      errorCode = ArgsException.ErrorCode.UNEXPECTED_ARGUMENT;\n      valid = false;\n    }\n  }\n\n  ..."}], "concepts": [{"id": "one-marshaler-per-type", "title": {"en": "One Marshaler per Type", "tr": "Tip Başına Tek Marshaler"}, "summary": {"en": "Each argument type (string, integer, double) should have its own marshaler class. Packing the parsing rules of many types into a single class gives it multiple reasons to change and makes every type hard to test on its own.", "tr": "Her argüman tipi (dizgi, tamsayı, kayan nokta) kendi marshaler sınıfına sahip olmalıdır. Birçok tipin ayrıştırma kurallarını tek sınıfa yığmak, bu sınıfa birden fazla değişme nedeni verir ve her bir tipin tek başına test edilmesini zorlaştırır."}, "bad": {"lang": "java", "code": "class AllInOneMarshaler {\n  private String stringValue = \"\";\n  private int intValue = 0;\n  private double doubleValue = 0.0;\n\n  void setString(String s) { stringValue = s; }\n\n  void setInt(String s) {\n    intValue = Integer.parseInt(s);\n  }\n\n  void setDouble(String s) {\n    doubleValue = Double.parseDouble(s);\n  }\n}", "why": {"en": "The parsing of string, int and double is mixed into one class: adding a new argument type (e.g. long) forces a change to this class, and a bug in one type's parsing endangers the others. The class has three reasons to change — a direct Single Responsibility Principle (SRP) violation.", "tr": "String, int ve double ayrıştırma tek sınıfta harmanlanmış: yeni bir argüman tipi (ör. long) eklemek bu sınıfı değiştirmeyi zorunlu kılıyor ve bir tipin ayrıştırma hatası diğerlerini de riske atıyor. Sınıfın değişmesi için üç nedeni var — Tek Sorumluluk İlkesi'nin (SRP) doğrudan ihlali."}}, "good": {"lang": "java", "code": "interface ArgumentMarshaler {\n  void set(Iterator<String> arg) throws ArgsException;\n  Object get();\n}\n\nclass StringArgumentMarshaler implements ArgumentMarshaler {\n  private String stringValue = \"\";\n  public void set(Iterator<String> arg) { stringValue = arg.next(); }\n  public Object get() { return stringValue; }\n}\n\nclass IntegerArgumentMarshaler implements ArgumentMarshaler {\n  private int intValue = 0;\n  public void set(Iterator<String> arg) {\n    intValue = Integer.parseInt(arg.next());\n  }\n  public Object get() { return intValue; }\n}", "why": {"en": "Each marshaler owns exactly one type, so it changes only when that type's rule changes. A new argument type joins as a new class implementing ArgumentMarshaler — the existing classes stay untouched (Open/Closed Principle) and each marshaler can be tested in isolation.", "tr": "Her marshaler tam olarak bir tipin sahibidir; dolayısıyla yalnızca o tipin kuralı değiştiğinde değişir. Yeni bir argüman tipi, ArgumentMarshaler'ı gerçekleştiren yeni bir sınıf olarak eklenir — mevcut sınıflara dokunulmaz (Açık Kapalı İlkesi) ve her marshaler izole olarak test edilebilir."}}, "tip": {"en": "When you find type checks inside a single class ('if it is a string parse it as a string, if an integer then...'), that is a signal to split the types into separate classes — one marshaler per type.", "tr": "Tek bir sınıfın içinde tip kontrolleri görürseniz ('dizgiyse öyle ayrıştır, tamsayıysa şöyle...'), bu tipleri ayrı sınıflara bölme sinyali — tip başına tek marshaler."}}, {"id": "separate-exception-module", "title": {"en": "ArgsException as Separate Module", "tr": "ArgsException Ayrı Modül Olarak"}, "summary": {"en": "An exception and its error codes should not be buried inside the class that throws them. Giving the exception its own file and responsibility keeps the main class focused on parsing and lets both sides evolve independently.", "tr": "Bir istisna ve hata kodları, onu fırlatan sınıfın içine gömülmemelidir. İstisnaya kendi dosyasını ve sorumluluğunu vermek, ana sınıfı ayrıştırma işinde odakta tutar ve iki tarafın bağımsız biçimde evrimleşmesine izin verir."}, "bad": {"lang": "java", "code": "public class Args {\n  public static class ArgsException extends Exception {\n    public enum ErrorCode {\n      OK, MISSING_STRING, MISSING_INTEGER, UNEXPECTED_ARGUMENT\n    }\n\n    public ArgsException() {}\n    public ArgsException(String message) { super(message); }\n  }\n\n  // ... 200 lines of schema and argument parsing logic ...\n}", "why": {"en": "The exception, its error codes and its state fields are nested inside a 200-line Args class: clients must reference it as Args.ArgsException, and every change to the parsing logic touches the same file as the error contract. Two responsibilities crammed into one class.", "tr": "İstisna, hata kodları ve durum alanları 200 satırlık Args sınıfının içine gömülü: istemciler onu Args.ArgsException olarak referanslamak zorunda kalıyor ve ayrıştırma mantığındaki her değişiklik, hata sözleşmesinin de bulunduğu aynı dosyaya dokunuyor. Tek sınıfa sıkıştırılmış iki sorumluluk."}}, "good": {"lang": "java", "code": "// ArgsException.java (own file)\npublic class ArgsException extends Exception {\n  private char errorArgumentId = '\\0';\n  private String errorParameter = \"TILT\";\n  private ErrorCode errorCode = ErrorCode.OK;\n\n  public enum ErrorCode {\n    OK, MISSING_STRING, MISSING_INTEGER, UNEXPECTED_ARGUMENT\n  }\n\n  public ArgsException() {}\n  public ArgsException(String message) { super(message); }\n}", "why": {"en": "ArgsException lives in its own file with a single job: representing an argument error. Args and all its clients simply import it; the parsing logic and the error contract can now change independently, and the exception stays readable at a glance.", "tr": "ArgsException, tek işi argüman hatasını temsil etmek olan kendi dosyasında yaşar. Args ve tüm istemcileri onu yalnızca import eder; ayrıştırma mantığı ile hata sözleşmesi artık bağımsız biçimde değişebilir ve istisna bir bakışta okunur durumda kalır."}}, "tip": {"en": "If a nested class is used outside its outer class — imported or referenced by other files — that is a signal it deserves its own top-level class and file.", "tr": "İç içe (nested) bir sınıf dışındaki sınıflar tarafından kullanılıyorsa — başka dosyalarca import ediliyorsa ya da referanslanıyorsa — o sınıfın kendi üst seviye sınıfına ve dosyasına çıkması gerektiğinin sinyalidir."}}]});
