window.PAGE({"id": "page-50", "page": 50, "pdf_page": 81, "chapter": {"num": 3, "en": "Functions", "tr": "Fonksiyonlar"}, "section": {"en": "Conclusion", "tr": "Sonuç"}, "title": {"en": "Conclusion & Listing 3-7", "tr": "Sonuç ve Listing 3-7"}, "blocks": [{"type": "para", "sentences": [{"en": "never forget that your real goal is to tell the story of the system, and that the functions you write need to fit cleanly together into a clear and precise language to help you with that telling.", "tr": "asıl amacınızın sistemin hikayesini anlatmak olduğunu ve yazdığınız fonksiyonların, bu anlatıma yardımcı olacak açık ve kesin bir dile temiz biçimde uyması gerektiğini asla unutmayın."}]}, {"type": "heading", "level": 1, "en": "SetupTeardownIncluder", "tr": "SetupTeardownIncluder"}, {"type": "caption", "kind": "listing", "en": "Listing 3-7 SetupTeardownIncluder.java", "tr": "Listing 3-7 SetupTeardownIncluder.java"}, {"type": "code", "lang": "java", "code": "package fitnesse.html;\n\nimport fitnesse.responders.run.SuiteResponder;\nimport fitnesse.wiki.*;\n\npublic class SetupTeardownIncluder {\n  private PageData pageData;\n  private boolean isSuite;\n  private WikiPage testPage;\n  private StringBuffer newPageContent;\n  private PageCrawler pageCrawler;\n\n  public static String render(PageData pageData) throws Exception {\n    return render(pageData, false);\n  }\n\n  public static String render(PageData pageData, boolean isSuite)\n    throws Exception {\n    return new SetupTeardownIncluder(pageData).render(isSuite);\n  }\n\n  private SetupTeardownIncluder(PageData pageData) {\n    this.pageData = pageData;\n    testPage = pageData.getWikiPage();\n    pageCrawler = testPage.getPageCrawler();\n    newPageContent = new StringBuffer();\n  }\n\n  private String render(boolean isSuite) throws Exception {\n    this.isSuite = isSuite;\n    if (isTestPage())\n      includeSetupAndTeardownPages();\n    return pageData.getHtml();\n  }\n\n  private boolean isTestPage() throws Exception {\n    return pageData.hasAttribute(\"Test\");\n  }\n\n  private void includeSetupAndTeardownPages() throws Exception {\n    includeSetupPages();\n    includePageContent();\n    includeTeardownPages();\n    updatePageContent();\n  }"}], "concepts": [{"id": "stepdown-rule-order-processing", "title": {"en": "The Stepdown Rule", "tr": "Basamak Kuralı (Stepdown Rule)"}, "summary": {"en": "A function should read like a top-down narrative: each function is followed by those at the next level of abstraction, so the reader descends one level at a time.", "tr": "Bir fonksiyon yukarıdan aşağıya okunan bir anlatı gibi olmalıdır: her fonksiyonu, bir alt soyutlama seviyesindeki fonksiyonlar izler; böylece okuyucu her seferinde tek bir seviye iner."}, "bad": {"lang": "java", "code": "public void processOrder(Order order) {\n  if (order.getItems().isEmpty())\n    throw new IllegalArgumentException(\"empty order\");\n  double total = 0;\n  for (Item item : order.getItems())\n    total += item.getPrice() * item.getQuantity();\n  paymentGateway.charge(order.getCustomer().getCard(), total);\n  inventory.reduceStock(order.getItems());\n  emailService.send(order.getCustomer().getEmail(), \"Order confirmed\");\n}", "why": {"en": "Everything happens at the same level of detail inside one function, so the reader cannot tell the big steps apart from the small ones.", "tr": "Tek bir fonksiyon içinde her şey aynı ayrıntı seviyesinde gerçekleşir; bu yüzden okuyucu büyük adımları küçüklerinden ayırt edemez."}}, "good": {"lang": "java", "code": "public void processOrder(Order order) {\n  validateOrder(order);\n  chargeCustomer(order);\n  updateInventory(order);\n  sendConfirmation(order);\n}", "why": {"en": "The top-level function reads like a summary; each named step can be understood on its own, one abstraction level down.", "tr": "Üst seviyedeki fonksiyon bir özet gibi okunur; her adı konmuş adım, bir alt soyutlama seviyesinde kendi başına anlaşılabilir."}}, "tip": {"en": "Ask yourself whether the next function down is at the same level of abstraction as the one before it — if not, split it further.", "tr": "Bir sonraki fonksiyonun, kendisinden öncekiyle aynı soyutlama seviyesinde olup olmadığını sorun; değilse fonksiyonu daha da bölün."}}, {"id": "private-constructor-static-factory", "title": {"en": "Private Constructor with a Static Factory Method", "tr": "Özel Kurucu ile Statik Fabrika Metodu (Static Factory Method)"}, "summary": {"en": "A class can hide its construction behind public static methods and keep the constructor private, so callers only see a simple, intention-revealing entry point.", "tr": "Bir sınıf, kurucusunu private tutup yapım işini public statik metotların arkasına gizleyebilir; böylece çağıranlar yalnızca basit ve niyetini belli eden bir giriş noktası görür."}, "bad": {"lang": "python", "code": "class ReportBuilder:\n    def __init__(self, data, formatter, filters):\n        self.data = data\n        self.formatter = formatter\n        self.filters = filters\n\n    def build(self):\n        filtered = self.filters.apply(self.data)\n        return self.formatter.format(filtered)\n\n# caller has to know the wiring\nbuilder = ReportBuilder(raw_data, HtmlFormatter(), DefaultFilters())\nreport = builder.build()", "why": {"en": "The caller must know which formatter and filters to pass in, exposing internal wiring that has nothing to do with the caller's actual intent.", "tr": "Çağıran, hangi formatter ve filters nesnelerini geçireceğini bilmek zorunda kalır; bu da çağıranın gerçek niyetiyle ilgisi olmayan iç bağlantıları dışarı sızdırır."}}, "good": {"lang": "python", "code": "class ReportBuilder:\n    def __init__(self, data, formatter, filters):\n        self._data = data\n        self._formatter = formatter\n        self._filters = filters\n\n    @staticmethod\n    def build_html_report(data):\n        return ReportBuilder(data, HtmlFormatter(), DefaultFilters())._build()\n\n    def _build(self):\n        filtered = self._filters.apply(self._data)\n        return self._formatter.format(filtered)\n\nreport = ReportBuilder.build_html_report(raw_data)", "why": {"en": "build_html_report reveals the caller's intent directly, while the private constructor and helper hide exactly how the object is assembled.", "tr": "build_html_report çağıranın niyetini doğrudan ortaya koyarken, private kurucu ve yardımcı metot nesnenin tam olarak nasıl kurulduğunu gizler."}}, "tip": {"en": "Use this pattern when a class has more than one way to be created, or when construction needs steps that shouldn't leak into every call site.", "tr": "Bu deseni, bir sınıfın birden fazla oluşturulma biçimi olduğunda ya da yapım aşamasının her çağrı noktasına sızmaması gerektiğinde kullanın."}}, {"id": "code-as-narrative", "title": {"en": "Code as Narrative", "tr": "Anlatım Olarak Kod (Code as Narrative)"}, "summary": {"en": "Well-named functions, composed of well-named smaller functions, let the reader follow the system's logic the way they would follow a story, without needing comments to explain what is happening.", "tr": "İyi isimlendirilmiş fonksiyonlar, yine iyi isimlendirilmiş daha küçük fonksiyonlardan oluştuğunda, okuyucu sistemin mantığını neler olduğunu açıklayan yorumlara ihtiyaç duymadan bir hikaye izler gibi takip edebilir."}, "bad": {"lang": "javascript", "code": "function handle(req, res) {\n  // check auth\n  if (!req.headers.authorization) { res.status(401).end(); return; }\n  const token = req.headers.authorization.split(\" \")[1];\n  const user = jwt.verify(token, SECRET);\n  // load and merge cart\n  const cart = db.carts.find(user.id);\n  const items = cart.items.map(i => ({ ...i, price: catalog.priceOf(i.sku) }));\n  res.json({ items, total: items.reduce((s, i) => s + i.price, 0) });\n}", "why": {"en": "The comments exist only because the code itself does not say what each block does; the story has to be read line by line.", "tr": "Yorumlar yalnızca kodun kendisi her bloğun ne yaptığını söylemediği için vardır; hikaye satır satır okunmak zorunda kalır."}}, "good": {"lang": "javascript", "code": "function handle(req, res) {\n  const user = authenticate(req);\n  if (!user) return rejectUnauthorized(res);\n  const cart = loadPricedCart(user);\n  res.json(cart);\n}", "why": {"en": "Each call names the next chapter of the story; a reader can stop at whatever level of detail they need.", "tr": "Her çağrı, hikayenin bir sonraki bölümünü adlandırır; okuyucu ihtiyaç duyduğu ayrıntı seviyesinde durabilir."}}, "tip": {"en": "When you feel the urge to write a comment explaining a block of code, try extracting that block into a well-named function instead.", "tr": "Bir kod bloğunu açıklamak için yorum yazma isteği duyduğunuzda, önce o bloğu iyi isimlendirilmiş bir fonksiyona çıkarmayı deneyin."}}]});
