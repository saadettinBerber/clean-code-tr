window.PAGE({"id": "page-223", "page": 223, "pdf_page": 223, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Same Change for Strings and Integers", "tr": "Strings ve Integers İçin Aynı Değişiklik"}, "blocks": [{"type": "code", "lang": "java", "code": "    return true;\n  }\n...\n  private void setBooleanArg(ArgumentMarshaler m) {\n    try {\n     m.set(\"true\");// was: booleanArgs.get(argChar).set(\"true\");\n    } catch (ArgsException e) {\n    }\n  }"}, {"type": "para", "sentences": [{"en": "The tests still passed, so I did the same with `Strings` and `Integers`.", "tr": "Testler hâlâ geçti, bu yüzden `Strings` ve `Integers` ile de aynı şeyi yaptım."}, {"en": "This allowed me to integrate some of the ugly exception management code into the `setArgument` function.", "tr": "Bu, bazı çirkin istisna yönetimi (exception management) kodunu `setArgument` fonksiyonuna entegre etmeme izin verdi."}]}, {"type": "code", "lang": "java", "code": "  private boolean setArgument(char argChar) throws ArgsException {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    try {\n      if (m instanceof BooleanArgumentMarshaler)\n        setBooleanArg(m);\n      else if (m instanceof StringArgumentMarshaler)\n        setStringArg(m);\n      else if (m instanceof IntegerArgumentMarshaler)\n        setIntArg(m);\n      else\n        return false;\n    } catch (ArgsException e) {\n     valid = false;\n      errorArgumentId = argChar;\n      throw e;\n    }\n    return true;\n  }\n\n  private void setIntArg(ArgumentMarshaler m) throws ArgsException {\n    currentArgument++;\n    String parameter = null;\n    try {\n      parameter = args[currentArgument];\n     m.set(parameter);\n    } catch (ArrayIndexOutOfBoundsException e) {\n      errorCode = ErrorCode.MISSING_INTEGER;\n      throw new ArgsException();\n    } catch (ArgsException e) {\n      errorParameter = parameter;\n      errorCode = ErrorCode.INVALID_INTEGER;\n      throw e;\n    }\n  }\n\n  private void setStringArg(ArgumentMarshaler m) throws ArgsException {\n    currentArgument++;\n    try {\n     m.set(args[currentArgument]);\n    } catch (ArrayIndexOutOfBoundsException e) {\n      errorCode = ErrorCode.MISSING_STRING;\n      throw new ArgsException();\n    }\n  }"}], "concepts": [{"id": "symmetric-refactor-across-types", "title": {"en": "Symmetric Refactor Across Types", "tr": "Tipler Arası Simetrik Yeniden Düzenleme"}, "summary": {"en": "When a design change affects a family of similar types, applying it to only one type leaves two competing designs in the same codebase. The change must be applied symmetrically to every type, each verified by its own tests before the next one is touched.", "tr": "Bir tasarım değişikliği benzer tiplerin oluşturduğu bir aileyi etkilediğinde, değişikliği yalnızca bir tipe uygulamak aynı kod tabanında iki birbirine rakip tasarım bırakır. Değişiklik, her biri sıradakine geçilmeden önce kendi testleriyle doğrulanmış biçimde, her tipe simetrik olarak uygulanmalıdır."}, "bad": {"lang": "java", "code": "  private void setArg(char argChar) {\n    if (booleanArgs.containsKey(argChar))\n      booleanArgs.get(argChar).set(true); // new path\n    else if (strings.containsKey(argChar))\n      strings.get(argChar).set(args[++currentArgument]); // old path\n    else if (integers.containsKey(argChar))\n      integers.get(argChar).set(Integer.parseInt(args[++currentArgument])); // old path\n  }", "why": {"en": "Only the boolean path was migrated to the new design; the string and integer paths still run on the old maps, so two mechanisms coexist in a single function.", "tr": "Yalnızca boolean yolu yeni tasarıma taşındı; string ve integer yolları hâlâ eski haritaların üzerinde çalışıyor, tek bir fonksiyonda iki mekanizma bir arada kalıyor."}}, "good": {"lang": "java", "code": "  private void setArg(char argChar) {\n    ArgumentMarshaler m = marshalers.get(argChar);\n    if (m instanceof BooleanArgumentMarshaler)\n      setBooleanArg(m);\n    else if (m instanceof StringArgumentMarshaler)\n      setStringArg(m);\n    else if (m instanceof IntegerArgumentMarshaler)\n      setIntArg(m);\n  }", "why": {"en": "The same change is applied to every type through one shared map; each type was migrated one at a time and its tests passed before the next type was touched.", "tr": "Aynı değişiklik, ortak tek bir harita üzerinden her tipe uygulanır; her tip tek tek taşınır ve sıradaki tipe dokunulmadan önce testleri geçer."}}, "tip": {"en": "Before checking in a design change, ask: are all the symmetric types treated the same way? A half-migrated family of types is a bug magnet.", "tr": "Bir tasarım değişikliğini teslim etmeden önce kendinize sorun: simetrik tüm tipler aynı mı muamele görüyor? Yarım kalmış bir tip ailesi hata mıknatısıdır."}}, {"id": "integration-as-a-step", "title": {"en": "Integration as a Step", "tr": "Birleştirme Bir Adım Olarak"}, "summary": {"en": "Integrating a new mechanism into existing code is itself a refactoring step. It should be a small, isolated change with its own green test run — not a big-bang merge of several untested changes.", "tr": "Yeni bir mekanizmayı mevcut koda entegre etmek (integrate) kendisi bir yeniden düzenleme (refactoring) adımıdır. Denenmemiş birkaç değişikliğin büyük patamalı (big-bang) birleştirilmesi değil, kendi yeşil test çalıştırmasına sahip küçük ve izole bir değişiklik olmalıdır."}, "bad": {"lang": "python", "code": "def set_arg(name):\n    # big-bang merge: boolean, string and int paths\n    # are all rewired in one untested change\n    if name in BOOLS:\n        return BOOLS[name].set(True)\n    if name in STRINGS:\n        return STRINGS[name].set(raw(next_token()))\n    if name in INTS:\n        return INTS[name].set(int(raw(next_token())))", "why": {"en": "All three type paths are rewired to the new maps in one giant change; when a test fails, there is no way to tell which type's integration broke it.", "tr": "Üç tip yolunun tümü, tek devasa değişiklikte yeni haritalara bağlanır; bir test başarısız olduğunda değişikliği hangi tipin entegrasyonunun (integration) bozduğunu anlamak imkânsız."}}, "good": {"lang": "python", "code": "# step 1: integrate only booleans, then run the tests\ndef set_arg(name):\n    if name in BOOLS:\n        return BOOLS[name].set(True)\n    if name in STRINGS:\n        return legacy_set_string(name)  # untouched\n    if name in INTS:\n        return legacy_set_int(name)     # untouched\n# green? -> step 2: integrate strings, run the tests\n# green? -> step 3: integrate ints, run the tests", "why": {"en": "Each integration is its own small change: one type per step, the rest left on the old path, and the whole suite green after every step — a failure always points at one small change.", "tr": "Her entegrasyon (integration) kendi başına küçük bir adımdır: adım başına bir tip, diğerleri eski yolda bırakılır ve her adımdan sonra tüm test paketi yeşildir — bir başarısızlık her zaman tek bir küçük değişikliğe işaret eder."}}, "tip": {"en": "Make integration a verb, not an event: integrate one slice, run the tests, commit; then the next slice.", "tr": "Entegrasyonu (integration) bir olay değil bir fiil yapın: bir dilimi entegre edin, testleri çalıştırın, commit edin; sonra bir sonraki dilim."}}]});
