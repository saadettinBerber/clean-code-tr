window.PAGE({"id": "page-91", "page": 91, "pdf_page": 91, "chapter": {"num": 5, "en": "Formatting", "tr": "Biçimlendirme"}, "section": {"en": "Uncle Bob's Formatting Rules", "tr": "Uncle Bob'un Biçimlendirme Kuralları"}, "title": {"en": "Listing 5-6: CodeAnalyzer.java", "tr": "Listing 5-6: CodeAnalyzer.java"}, "blocks": [{"type": "para", "sentences": [{"en": "Listing 5-6 CodeAnalyzer.java", "tr": "Listing 5-6 CodeAnalyzer.java"}]}, {"type": "caption", "kind": "listing", "en": "Listing 5-6 CodeAnalyzer.java", "tr": "Listing 5-6 CodeAnalyzer.java"}, {"type": "code", "lang": "java", "code": "public class CodeAnalyzer implements JavaFileAnalysis {\n  private int lineCount;\n  private int maxLineWidth;\n  private int widestLineNumber;\n  private LineWidthHistogram lineWidthHistogram;\n  private int totalChars;\n\n  public CodeAnalyzer() {\n    lineWidthHistogram = new LineWidthHistogram();\n  }\n\n  public static List<File> findJavaFiles(File parentDirectory) {\n    List<File> files = new ArrayList<File>();\n    findJavaFiles(parentDirectory, files);\n    return files;\n  }\n\n  private static void findJavaFiles(File parentDirectory, List<File> files) {\n    for (File file : parentDirectory.listFiles()) {\n      if (file.getName().endsWith(\".java\"))\n        files.add(file);\n      else if (file.isDirectory())\n        findJavaFiles(file, files);\n    }\n  }\n\n  public void analyzeFile(File javaFile) throws Exception {\n    BufferedReader br = new BufferedReader(new FileReader(javaFile));\n    String line;\n    while ((line = br.readLine()) != null)\n      measureLine(line);\n  }\n\n  private void measureLine(String line) {\n    lineCount++;\n    int lineSize = line.length();\n    totalChars += lineSize;\n    lineWidthHistogram.addLine(lineSize, lineCount);\n    recordWidestLine(lineSize);\n  }\n\n  private void recordWidestLine(int lineSize) {\n    if (lineSize > maxLineWidth) {\n      maxLineWidth = lineSize;\n      widestLineNumber = lineCount;\n    }\n  }\n\n  public int getLineCount() {\n    return lineCount;\n  }\n\n  public int getMaxLineWidth() {\n    return maxLineWidth;\n  }"}], "concepts": [{"id": "code-as-the-real-formatting-standard", "title": {"en": "Let the Code Itself Be the Standard", "tr": "Kodun Kendisi Standart Olsun"}, "summary": {"en": "Instead of writing a prose formatting standard, Uncle Bob points to a single well-formatted class, CodeAnalyzer.java, as the example: a short file, fields grouped at the top, and methods kept in the order a reader would naturally encounter them.", "tr": "Uncle Bob, düzyazı halinde bir biçimlendirme standardı yazmak yerine, örnek olarak tek bir iyi biçimlendirilmiş sınıfı — CodeAnalyzer.java — gösterir: kısa bir dosya, en üstte gruplanmış alanlar ve bir okuyucunun doğal olarak karşılaşacağı sırada tutulan metotlar."}, "bad": {"lang": "java", "code": "public class ReportBuilder {\n  public String footer() { return buildFooter(); }\n\n  private String title;\n\n  public void run() {\n    print(header());\n    print(body());\n    print(footer());\n  }\n\n  private int pageCount;\n\n  private String header() { return buildHeader(); }\n  private String body() { return buildBody(); }\n}", "why": {"en": "Fields and methods are interleaved with no logical order, so a reader has to hunt back and forth to find `run`'s helpers or the state they touch.", "tr": "Alanlar ve metotlar hiçbir mantıksal sıra olmadan iç içe geçmiş, bu yüzden okuyucu `run`'ın yardımcılarını ya da dokundukları durumu bulmak için ileri geri arama yapmak zorunda kalır."}}, "good": {"lang": "java", "code": "public class ReportBuilder {\n  private String title;\n  private int pageCount;\n\n  public void run() {\n    print(header());\n    print(body());\n    print(footer());\n  }\n\n  private String header() { return buildHeader(); }\n  private String body() { return buildBody(); }\n  private String footer() { return buildFooter(); }\n}", "why": {"en": "Fields are grouped right after the class declaration, and the public entry point `run` is followed by its helpers in the order they are called, so the class reads top to bottom.", "tr": "Alanlar sınıf tanımından hemen sonra gruplanır ve genel giriş noktası `run`, çağrıldıkları sırayla yardımcı metotlarınca izlenir; böylece sınıf baştan sona okunabilir."}}, "tip": {"en": "Don't just write a formatting policy document — format one real class the way you want every class formatted, and point people at it.", "tr": "Sadece bir biçimlendirme politikası belgesi yazmayın — gerçek bir sınıfı her sınıfın biçimlendirilmesini istediğiniz şekilde biçimlendirin ve insanlara onu gösterin."}}, {"id": "instance-variables-grouped-at-top", "title": {"en": "Group Instance Variables at the Top of the Class", "tr": "Örnek Değişkenlerini (Instance Variables) Sınıfın Başında Grupla"}, "summary": {"en": "In CodeAnalyzer, all five instance variables — lineCount, maxLineWidth, widestLineNumber, lineWidthHistogram, totalChars — are declared together immediately after the class opens, before any method, so the reader sees the object's full state at a glance.", "tr": "CodeAnalyzer sınıfında beş örnek değişkeninin tümü — lineCount, maxLineWidth, widestLineNumber, lineWidthHistogram, totalChars — sınıf açıldıktan hemen sonra, herhangi bir metottan önce birlikte tanımlanır; böylece okuyucu nesnenin tüm durumunu tek bakışta görür."}, "bad": {"lang": "javascript", "code": "class Cart {\n  addItem(item) {\n    this.items.push(item);\n  }\n\n  items = [];\n\n  applyDiscount(percent) {\n    this.discountPercent = percent;\n  }\n\n  discountPercent = 0;\n}", "why": {"en": "Each field is declared right before the method that first uses it, so understanding the object's shape means reading the whole class.", "tr": "Her alan, onu ilk kullanan metodun hemen öncesinde tanımlanmış; bu yüzden nesnenin yapısını anlamak sınıfın tamamını okumayı gerektirir."}}, "good": {"lang": "javascript", "code": "class Cart {\n  items = [];\n  discountPercent = 0;\n\n  addItem(item) {\n    this.items.push(item);\n  }\n\n  applyDiscount(percent) {\n    this.discountPercent = percent;\n  }\n}", "why": {"en": "Both fields are declared together at the top, giving an immediate summary of the object's state before the reader gets to any behavior.", "tr": "İki alan da en üstte birlikte tanımlanır; bu, okuyucu herhangi bir davranışa geçmeden önce nesnenin durumu hakkında anında bir özet sunar."}}, "tip": {"en": "Whichever convention you pick — fields at the top or the bottom — apply it consistently across the whole codebase.", "tr": "Hangi kuralı seçerseniz seçin — alanları en üste ya da en alta koymak — bunu tüm kod tabanında tutarlı biçimde uygulayın."}}, {"id": "caller-above-callee-real-example", "title": {"en": "The Caller Sits Right Above the Callee", "tr": "Çağıran, Çağrılanın Hemen Üstünde Durur"}, "summary": {"en": "On this very page, the public `findJavaFiles(File)` is declared first and immediately followed by the private overload `findJavaFiles(File, List)` that it delegates to — a direct, real example of the Vertical Ordering and Stepdown Rule ideas from earlier in this chapter.", "tr": "Bu sayfada, genel `findJavaFiles(File)` önce tanımlanır ve hemen ardından iş devrettiği özel aşırı yüklenmiş metot `findJavaFiles(File, List)` gelir — bu, bölümün önceki kısımlarındaki Dikey Sıralama (Vertical Ordering) ve Basamak Kuralı (Stepdown Rule) fikirlerinin doğrudan, gerçek bir örneğidir."}, "bad": {"lang": "python", "code": "def _flatten(node, acc):\n    for child in node.children:\n        _flatten(child, acc)\n    acc.append(node)\n\ndef unrelated_helper():\n    pass\n\ndef flatten_tree(root):\n    acc = []\n    _flatten(root, acc)\n    return acc", "why": {"en": "The private helper `_flatten` and an unrelated function sit above the public `flatten_tree` that actually calls it, forcing the reader to scroll down and then back up.", "tr": "Özel yardımcı `_flatten` ve ilgisiz bir fonksiyon, onu asıl çağıran genel `flatten_tree`'nin üstünde durur; bu da okuyucuyu aşağı kaydırıp sonra tekrar yukarı çıkmaya zorlar."}}, "good": {"lang": "python", "code": "def flatten_tree(root):\n    acc = []\n    _flatten(root, acc)\n    return acc\n\ndef _flatten(node, acc):\n    for child in node.children:\n        _flatten(child, acc)\n    acc.append(node)", "why": {"en": "The public function comes first, and its private helper follows immediately below, matching the natural order in which a reader would want to explore the code.", "tr": "Genel fonksiyon önce gelir ve özel yardımcısı hemen altında izler; bu, bir okuyucunun kodu keşfetmek isteyeceği doğal sırayla eşleşir."}}, "tip": {"en": "When a public method delegates to a private overload or helper, place that helper directly beneath it — don't make the reader search for it.", "tr": "Genel bir metot özel bir aşırı yüklenmiş metoda ya da yardımcıya iş devrettiğinde, o yardımcıyı doğrudan altına yerleştirin — okuyucuyu onu aramaya zorlamayın."}}]});
