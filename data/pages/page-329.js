window.PAGE({"id": "page-329", "page": 329, "pdf_page": 329, "chapter": {"num": 18, "en": "Appendices", "tr": "Ekler"}, "section": {"en": "Dependencies Between Methods Can Break Concurrent Code", "tr": "Metotlar Arasındaki Bağımlılıklar Eşzamanlı Kodu Bozabilir (Dependencies Between Methods Can Break Concurrent Code)"}, "title": {"en": "Thread-Safe Collections & Dependencies Between Methods", "tr": "İş Parçacığına Güvenli Koleksiyonlar ve Metotlar Arasındaki Bağımlılıklar"}, "blocks": [{"type": "para", "sentences": [{"en": "Each individual method is thread-safe.", "tr": "Her bir metot (method) tek başına iş parçacığına güvenlidir (thread-safe)."}, {"en": "However, another thread might add a value in between the `containsKey` and put calls.", "tr": "Bununla birlikte, `containsKey` ve put çağrıları arasında başka bir iş parçacığı (thread) bir değer ekleyebilir."}, {"en": "There are several options to fix this problem.", "tr": "Bu sorunu gidermek için birkaç seçenek (option) vardır."}]}, {"type": "list", "ordered": false, "items": [{"en": "Lock the `HashTable` first, and make sure all other users of the `HashTable` do the sameclient-based locking:", "tr": "Önce `HashTable`'ı kilitleyin (lock) ve `HashTable`'ın diğer tüm kullanıcılarının da aynısını yaptığından emin olun — istemci tabanlı kilitleme (client-based locking):"}, {"en": "Wrap the `HashTable` in its own object and use a different API—server-based locking using an ADAPTER:", "tr": "`HashTable`'ı kendi nesnesinin (object) içine sarın (wrap) ve farklı bir API kullanın — bir ADAPTER (Adaptör Deseni) kullanan sunucu tabanlı kilitleme (server-based locking):"}, {"en": "Use the thread-safe collections:", "tr": "İş parçacığına güvenli koleksiyonları (thread-safe collections) kullanın:"}]}, {"type": "code", "lang": "java", "code": "ConcurrentHashMap<Integer, String> map = new ConcurrentHashMap<Integer,\nString>();\nmap.putIfAbsent(key, value);"}, {"type": "para", "sentences": [{"en": "The collections in `java.util.concurrent` have operations like `putIfAbsent`() to accommodate such operations.", "tr": "`java.util.concurrent` içindeki koleksiyonlar, bu tür işlemleri (operations) karşılamak için `putIfAbsent`() gibi işlemlere sahiptir."}]}, {"type": "heading", "level": 1, "en": "Dependencies Between Methods Can Break Concurrent Code", "tr": "Metotlar Arasındaki Bağımlılıklar Eşzamanlı Kodu Bozabilir (Dependencies Between Methods Can Break Concurrent Code)"}, {"type": "para", "sentences": [{"en": "Here is a trivial example of a way to introduce dependencies between methods:", "tr": "İşte metotlar arasında bağımlılıklar (dependencies) oluşturmanın bir yolunun önemsiz (trivial) bir örneği:"}]}, {"type": "code", "lang": "java", "code": "public class IntegerIterator implements Iterator<Integer>\n    private Integer nextValue = 0;\n\n    public synchronized boolean hasNext() {\n        return nextValue < 100000;\n    }\n    public synchronized Integer next() {\n        if (nextValue == 100000)\n            throw new IteratorPastEndException();\n        return nextValue++;\n    }\n    public synchronized Integer getNextValue() {\n        return nextValue;\n    }\n}"}, {"type": "para", "sentences": [{"en": "Here is some code to use this `IntegerIterator`:", "tr": "İşte bu `IntegerIterator`'ı kullanmak için bazı kod:"}]}, {"type": "code", "lang": "java", "code": "IntegerIterator iterator = new IntegerIterator();\nwhile(iterator.hasNext()) {"}], "concepts": [{"id": "atomic-check-then-act", "kind": "code", "title": {"en": "Make Check-Then-Act Atomic with putIfAbsent", "tr": "Kontrol-Et-Sonra-Eylemi putIfAbsent ile Atomik Yapın (Make Check-Then-Act Atomic with putIfAbsent)"}, "summary": {"en": "Two separate calls (containsKey, then put) leave a gap in which another thread can insert a value. A concurrent collection turns the check and the insert into one atomic operation with putIfAbsent, so the decision and the update cannot be separated by another thread.", "tr": "Birbirini izleyen iki ayrı çağrı (önce containsKey, sonra put), aralarına başka bir iş parçacığının değer ekleyebileceği bir boşluk bırakır. Eşzamanlı bir koleksiyon (concurrent collection), kontrolü ve eklemeyi putIfAbsent ile tek bir atomik işleme (atomic operation) dönüştürür; böylece karar ile güncelleme başka bir iş parçacığı tarafından birbirinden ayrılamaz."}, "bad": {"lang": "java", "code": "// fine only when no other thread touches the map\nif (!map.containsKey(key)) {\n    map.put(key, value);   // a thread can add in between\n}", "why": {"en": "The containsKey call and the put call are two separate operations. Another thread can insert a value between them, so two threads can both pass the check and the second put silently replaces the first value.", "tr": "containsKey çağrısı ile put çağrısı iki ayrı işlemdir. Başka bir iş parçacığı bu ikisinin arasına bir değer ekleyebilir; böylece iki iş parçacığı da kontrolü geçebilir ve ikinci put, birinci değerin üzerine sessizce yazar."}}, "good": {"lang": "java", "code": "String previous = map.putIfAbsent(key, value);\nif (previous != null) {\n    // a value was already present — handle the collision\n}", "why": {"en": "putIfAbsent is a single atomic step on a concurrent collection: it inserts only if the key is absent and returns the existing value otherwise. No lock is needed and no gap exists for another thread to slip through.", "tr": "putIfAbsent, eşzamanlı bir koleksiyonda tek bir atomik adımdır: anahtar yoksa değeri ekler, varsa mevcut değeri döndürür. Kilide (lock) gerek yoktur ve başka bir iş parçacığının araya sızabileceği bir boşluk yoktur."}}, "tip": {"en": "Whenever you see the pattern \"check, then act\" (containsKey before put, peek before remove, and so on), look for an atomic variant such as putIfAbsent — fusing the two steps removes the race entirely.", "tr": "Ne zaman \"kontrol et, sonra eyle\" desenini (containsKey'den önce put, peek'ten önce remove vb.) görürseniz, putIfAbsent gibi atomik (atomic) bir varyant arayın — iki adımı birleştirmek yarış durumunu (race condition) tamamen ortadan kaldırır."}}, {"id": "server-based-locking-wrapper", "kind": "code", "title": {"en": "Own the Lock Inside the Server, Not in Every Client", "tr": "Kilidin Sahibi Sunucu Olsun, Her İstemci Değil (Own the Lock Inside the Server, Not in Every Client)"}, "summary": {"en": "Client-based locking asks every caller to remember to synchronize on the shared object; one forgotten lock breaks the whole guarantee. Wrapping the object in its own class with a purpose-built API — server-based locking via an adapter — keeps the locking policy in exactly one place.", "tr": "İstemci tabanlı kilitleme (client-based locking), her çağıranın paylaşılan nesneyi senkronize etmeyi hatırlamasını ister; unutulan tek bir kilit, güvenceyi bütünüyle bozar. Nesneyi, amacına uygun bir API ile kendi sınıfına sarmak (wrap) — bir adaptör (adapter) aracılığıyla sunucu tabanlı kilitleme (server-based locking) — kilitleme politikasını tam olarak tek bir yerde tutar."}, "bad": {"lang": "java", "code": "// every client must remember to lock the table\nsynchronized (table) {\n    if (!table.containsKey(key)) {\n        table.put(key, value);\n    }\n}", "why": {"en": "The correctness depends on every single caller synchronizing on the table. One client that forgets the synchronized block silently reintroduces the race, and the locking rule is documented nowhere in the API.", "tr": "Doğruluk, her bir çağıranın tablo üzerinde senkronize olmasına bağlıdır. synchronized bloğunu unutan tek bir istemci (client), yarış durumunu (race condition) sessizce geri getirir ve kilitleme kuralı API'nin hiçbir yerinde belgelenmemiştir."}}, "good": {"lang": "java", "code": "public class KeyedValueStore {\n    private final Hashtable<String, String> table =\n        new Hashtable<String, String>();\n\n    public synchronized void putIfAbsent(String key, String value) {\n        if (!table.containsKey(key)) {\n            table.put(key, value);\n        }\n    }\n}", "why": {"en": "The wrapper owns its table and its lock: the synchronized method covers the check and the insert together, and the locked policy lives in exactly one place. Callers can no longer get the locking wrong.", "tr": "Sarmalayıcı (wrapper), kendi tablosunun ve kilidinin (lock) sahibidir: synchronized metot, kontrolü ve eklemeyi birlikte kapsar ve kilitli politika tam olarak tek bir yerde yaşar. Çağıranlar artık kilitlemeyi yanlış yapamaz."}}, "tip": {"en": "If your API forces clients to remember a locking protocol, refactor it: give the data-owning object a method that locks internally (server-based locking) instead of publishing the shared object and hoping every caller behaves.", "tr": "API'niz istemcileri bir kilitleme protokolünü hatırlamaya zorluyorsa, onu yeniden düzenleyin (refactor): paylaşılan nesneyi yayınlayıp her çağıranın düzgün davranmasını ummak yerine, verinin sahibi olan nesneye içeride kilitlenen bir metot verin (sunucu tabanlı kilitleme)."}}, {"id": "method-dependencies-iterators", "kind": "code", "title": {"en": "Thread-Safe Methods Can Still Depend on Each Other", "tr": "İş Parçacığına Güvenli Metotlar Yine de Birbirine Bağımlı Olabilir (Thread-Safe Methods Can Still Depend on Each Other)"}, "summary": {"en": "hasNext() and next() are each synchronized, but a caller depends on their order: the iteration contract only holds if the two calls are performed as a group. Two threads sharing one IntegerIterator can both pass the last hasNext() check and drive one of them past the end.", "tr": "hasNext() ve next() metotlarının her biri ayrı ayrı senkronizedir, ancak bir çağıran onların sırasına bağımlıdır: yineleme (iteration) sözleşmesi, yalnızca iki çağrı bir grup olarak gerçekleştirilirse geçerlidir. Tek bir IntegerIterator'ı paylaşan iki iş parçacığı, son hasNext() kontrolünü birlikte geçebilir ve ikisinden birini listenin sonunun ötesine sürükleyebilir."}, "bad": {"lang": "java", "code": "IntegerIterator iterator = new IntegerIterator();\nwhile (iterator.hasNext()) {\n    int value = iterator.next();   // two threads share this\n}", "why": {"en": "Each method is synchronized, but nothing protects the dependency between the two calls. At the end of the iteration both threads can pass the true hasNext() before either calls next(), so one thread goes beyond the end of the list.", "tr": "Her metot senkronizedir, ancak iki çağrı arasındaki bağımlılığı (dependency) koruyan hiçbir şey yoktur. Yinelemenin sonunda, ikisinden biri next()'i çağırmadan önce iki iş parçacığı da true dönen hasNext() kontrolünden geçebilir; böylece biri listenin sonunun ötesine gider."}}, "good": {"lang": "java", "code": "AtomicInteger nextValue = new AtomicInteger(0);\nwhile (true) {\n    int value = nextValue.getAndIncrement();\n    if (value >= 100000) {\n        break;\n    }\n    process(value);\n}", "why": {"en": "getAndIncrement fuses the check and the fetch into a single atomic operation, so no gap exists between \"is there a next value?\" and \"give it to me\". The method dependency disappears; each element is seen exactly once.", "tr": "getAndIncrement, kontrolü ve değeri alma işlemini tek bir atomik işleme (atomic operation) kaynaştırır; böylece \"sıradaki değer var mı?\" ile \"onu bana ver\" arasında bir boşluk kalmaz. Metot bağımlılığı ortadan kalkar; her öğe tam olarak bir kez görülür."}}, "tip": {"en": "When methods must be called in a group (hasNext/next, isEmpty/size, peek/remove), never rely on each method being synchronized alone. Fuse the group into one atomic call or protect the whole sequence with a single lock.", "tr": "Metotlar bir grup olarak çağrılmak zorundayken (hasNext/next, isEmpty/size, peek/remove), her metodun tek tek senkronize olmasına asla güvenmeyin. Grubu tek bir atomik çağrıda kaynaştırın ya da tüm sırayı tek bir kilitle (lock) koruyun."}}]});
