window.PAGE({"id": "page-145", "page": 145, "pdf_page": 145, "chapter": {"num": 10, "en": "Classes", "tr": "Sınıflar"}, "section": {"en": "Maintaining Cohesion Results in Many Small Classes", "tr": "Bütünlüğü Korumak Çok Sayıda Küçük Sınıf Doğurur"}, "title": {"en": "Listing 10-8", "tr": "Listing 10-8"}, "blocks": [{"type": "para", "sentences": [{"en": "Listing 10-8 PrimeGenerator.java", "tr": "Listing 10-8 PrimeGenerator.java"}]}, {"type": "caption", "kind": "listing", "en": "Listing 10-8 PrimeGenerator.java", "tr": "Listing 10-8 PrimeGenerator.java"}, {"type": "code", "lang": "java", "code": "package literatePrimes;\n\nimport java.util.ArrayList;\n\npublic class PrimeGenerator {\n  private static int[] primes;\n  private static ArrayList<Integer> multiplesOfPrimeFactors;\n\n  protected static int[] generate(int n) {\n    primes = new int[n];\n    multiplesOfPrimeFactors = new ArrayList<Integer>();\n    set2AsFirstPrime();\n    checkOddNumbersForSubsequentPrimes();\n    return primes;\n  }\n\n  private static void set2AsFirstPrime() {\n    primes[0] = 2;\n    multiplesOfPrimeFactors.add(2);\n  }\n\n  private static void checkOddNumbersForSubsequentPrimes() {\n    int primeIndex = 1;\n    for (int candidate = 3;\n         primeIndex < primes.length;\n         candidate += 2) {\n      if (isPrime(candidate))\n        primes[primeIndex++] = candidate;\n    }\n  }\n\n  private static boolean isPrime(int candidate) {\n    if (isLeastRelevantMultipleOfNextLargerPrimeFactor(candidate)) {\n      multiplesOfPrimeFactors.add(candidate);\n      return false;\n    }\n    return isNotMultipleOfAnyPreviousPrimeFactor(candidate);\n  }\n\n  private static boolean\n  isLeastRelevantMultipleOfNextLargerPrimeFactor(int candidate) {\n    int nextLargerPrimeFactor = primes[multiplesOfPrimeFactors.size()];\n    int leastRelevantMultiple = nextLargerPrimeFactor * nextLargerPrimeFactor;\n    return candidate == leastRelevantMultiple;\n  }\n\n  private static boolean\n  isNotMultipleOfAnyPreviousPrimeFactor(int candidate) {\n    for (int n = 1; n < multiplesOfPrimeFactors.size(); n++) {\n      if (isMultipleOfNthPrimeFactor(candidate, n))\n        return false;\n    }"}], "concepts": [{"id": "named-steps-replace-comments", "title": {"en": "Well-Named Private Steps Replace the Need for Comments", "tr": "İyi Adlandırılmış Private Adımlar Yorumlara Olan İhtiyacın Yerini Alır"}, "summary": {"en": "Breaking a dense boolean check into several small, descriptively named private methods lets the sequence of calls document the logic on its own.", "tr": "Yoğun bir boole kontrolünü birkaç küçük ve betimleyici adı olan private metoda bölmek, çağrı sırasının mantığı kendi başına belgelemesini sağlar."}, "bad": {"lang": "java", "code": "public class DiscountEligibility {\n  public static boolean qualifies(Customer c) {\n    if (c.getYears() >= 5) return true;\n    if (c.getYears() >= 2 && c.getOrders() > 20) return true;\n    if (c.isVip() && c.getYears() >= 1) return true;\n    return false;\n  }\n}", "why": {"en": "The three rules are readable only if you mentally simulate each `if`; nothing in the code names what any of the three conditions actually mean.", "tr": "Üç kural yalnızca her `if`i zihninizde canlandırırsanız okunabilir; kod içinde bu üç koşuldan hiçbirinin ne anlama geldiğini adlandıran bir şey yok."}}, "good": {"lang": "java", "code": "public class DiscountEligibility {\n  public static boolean qualifies(Customer c) {\n    return isLoyalCustomer(c) || isFrequentBuyer(c) || isNewVip(c);\n  }\n\n  private static boolean isLoyalCustomer(Customer c) {\n    return c.getYears() >= 5;\n  }\n\n  private static boolean isFrequentBuyer(Customer c) {\n    return c.getYears() >= 2 && c.getOrders() > 20;\n  }\n\n  private static boolean isNewVip(Customer c) {\n    return c.isVip() && c.getYears() >= 1;\n  }\n}", "why": {"en": "`qualifies` now reads as a plain sentence, and each rule can be tested or changed on its own without touching the others.", "tr": "`qualifies` artık düz bir cümle gibi okunuyor ve her kural, diğerlerine dokunmadan tek başına test edilebilir ya da değiştirilebilir."}}, "tip": {"en": "If you feel the urge to write a comment explaining what a condition means, extract a private method with that meaning as its name instead.", "tr": "Bir koşulun ne anlama geldiğini açıklayan bir yorum yazma isteği duyarsanız, bunun yerine o anlamı isim olarak taşıyan bir private metot çıkarın."}}]});
