window.PAGE({"id": "page-144", "page": 144, "pdf_page": 144, "chapter": {"num": 10, "en": "Classes", "tr": "Sınıflar"}, "section": {"en": "Maintaining Cohesion Results in Many Small Classes", "tr": "Bütünlüğü Korumak Çok Sayıda Küçük Sınıf Doğurur"}, "title": {"en": "Listing 10-7 (continued)", "tr": "Listing 10-7 (devam)"}, "blocks": [{"type": "caption", "kind": "listing", "en": "Listing 10-7 (continued) RowColumnPagePrinter.java", "tr": "Listing 10-7 (devam) RowColumnPagePrinter.java"}, {"type": "code", "lang": "java", "code": "  public void print(int data[]) {\n    int pageNumber = 1;\n    for (int firstIndexOnPage = 0;\n         firstIndexOnPage < data.length;\n         firstIndexOnPage += numbersPerPage) {\n      int lastIndexOnPage =\n        Math.min(firstIndexOnPage + numbersPerPage - 1,\n                 data.length - 1);\n      printPageHeader(pageHeader, pageNumber);\n      printPage(firstIndexOnPage, lastIndexOnPage, data);\n      printStream.println(\"\\f\");\n      pageNumber++;\n    }\n  }\n\n  private void printPage(int firstIndexOnPage,\n                         int lastIndexOnPage,\n                         int[] data) {\n    int firstIndexOfLastRowOnPage =\n      firstIndexOnPage + rowsPerPage - 1;\n    for (int firstIndexInRow = firstIndexOnPage;\n         firstIndexInRow <= firstIndexOfLastRowOnPage;\n         firstIndexInRow++) {\n      printRow(firstIndexInRow, lastIndexOnPage, data);\n      printStream.println(\"\");\n    }\n  }\n\n  private void printRow(int firstIndexInRow,\n                        int lastIndexOnPage,\n                        int[] data) {\n    for (int column = 0; column < columnsPerPage; column++) {\n      int index = firstIndexInRow + column * rowsPerPage;\n      if (index <= lastIndexOnPage)\n        printStream.format(\"%10d\", data[index]);\n    }\n  }\n\n  private void printPageHeader(String pageHeader,\n                               int pageNumber) {\n    printStream.println(pageHeader + \" --- Page \" + pageNumber);\n    printStream.println(\"\");\n  }\n\n  public void setOutput(PrintStream printStream) {\n    this.printStream = printStream;\n  }\n}"}], "concepts": [{"id": "private-helpers-tell-the-story", "kind": "code", "title": {"en": "Private Helper Methods Let the Public Method Read Like a Summary", "tr": "Private Yardımcı Metotlar, Public Metodun Bir Özet Gibi Okunmasını Sağlar"}, "summary": {"en": "Delegating the details of a public method to a handful of well-named private helpers turns the public method into a short table of contents for what it does.", "tr": "Public bir metodun ayrıntılarını iyi adlandırılmış birkaç private yardımcı metoda devretmek, o public metodu yaptığı işin kısa bir içindekiler tablosuna dönüştürür."}, "bad": {"lang": "java", "code": "public class SeatingChartPrinter {\n  public void print(String[] names, int seatsPerRow) {\n    for (int row = 0; row * seatsPerRow < names.length; row++) {\n      StringBuilder line = new StringBuilder();\n      for (int col = 0; col < seatsPerRow; col++) {\n        int idx = row * seatsPerRow + col;\n        if (idx < names.length) line.append(names[idx]).append(\"\\t\");\n      }\n      System.out.println(line);\n    }\n  }\n}", "why": {"en": "The row-building logic and the printing logic are inlined together, so reading `print` means reading two nested loops at once.", "tr": "Satır oluşturma mantığı ile yazdırma mantığı iç içe yazılmış; bu yüzden `print`'i okumak, iki iç içe döngüyü aynı anda okumak anlamına geliyor."}}, "good": {"lang": "java", "code": "public class SeatingChartPrinter {\n  public void print(String[] names, int seatsPerRow) {\n    int rowCount = (names.length + seatsPerRow - 1) / seatsPerRow;\n    for (int row = 0; row < rowCount; row++) {\n      printRow(names, seatsPerRow, row);\n    }\n  }\n\n  private void printRow(String[] names, int seatsPerRow, int row) {\n    System.out.println(buildRow(names, seatsPerRow, row));\n  }\n\n  private String buildRow(String[] names, int seatsPerRow, int row) {\n    StringBuilder line = new StringBuilder();\n    for (int col = 0; col < seatsPerRow; col++) {\n      int idx = row * seatsPerRow + col;\n      if (idx < names.length) line.append(names[idx]).append(\"\\t\");\n    }\n    return line.toString();\n  }\n}", "why": {"en": "`print` now reads as 'for each row, print it' — the how of building a row is one level down, exactly where a reader can skip it if they don't need it.", "tr": "`print` artık 'her satır için, onu yazdır' şeklinde okunuyor — bir satırın nasıl oluşturulduğu bir seviye aşağıda, tam olarak bir okuyucunun ihtiyaç duymadığında atlayabileceği yerde."}}, "tip": {"en": "This is the Stepdown Rule at the method level: a public method should read top-to-bottom at one level of abstraction, with details pushed into named private methods below it.", "tr": "Bu, Basamak Kuralı'nın (Stepdown Rule) metot düzeyindeki hâlidir: public bir metot, baştan sona tek bir soyutlama seviyesinde okunmalı, ayrıntılar ise altındaki adlandırılmış private metotlara itilmelidir."}}]});
