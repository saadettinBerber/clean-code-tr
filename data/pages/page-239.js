window.PAGE({"id": "page-239", "page": 239, "pdf_page": 239, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Double Error Tests & Splitting ArgsException", "tr": "Double Hata Testleri ve ArgsException'ın Ayrıştırılması"}, "blocks": [{"type": "code", "lang": "java", "code": "    assertEquals(\"Argument -x expects a double but was 'Forty two'.\",\n                 args.errorMessage());\n  }\n---\n  public String errorMessage() throws Exception {\n    switch (errorCode) {\n      case OK:\n        throw new Exception(\"TILT: Should not get here.\");\n      case UNEXPECTED_ARGUMENT:\n        return unexpectedArgumentMessage();\n      case MISSING_STRING:\n        return String.format(\"Could not find string parameter for -%c.\",\n                             errorArgumentId);\n      case INVALID_INTEGER:\n        return String.format(\"Argument -%c expects an integer but was '%s'.\",\n                             errorArgumentId, errorParameter);\n      case MISSING_INTEGER:\n        return String.format(\"Could not find integer parameter for -%c.\",\n                             errorArgumentId);\n      case INVALID_DOUBLE:\n        return String.format(\"Argument -%c expects a double but was '%s'.\",\n                             errorArgumentId, errorParameter);\n      case MISSING_DOUBLE:\n        return String.format(\"Could not find double parameter for -%c.\",\n                             errorArgumentId);\n    }\n    return \"\";\n  }"}, {"type": "para", "sentences": [{"en": "And the tests pass.", "tr": "Ve testler geçti."}, {"en": "The next test makes sure we detect a missing double argument properly.", "tr": "Bir sonraki test, eksik bir double argümanını düzgün algıladığımızdan emin oluyor."}]}, {"type": "code", "lang": "java", "code": "  public void testMissingDouble() throws Exception {\n    Args args = new Args(\"x##\", new String[]{\"-x\"});\n    assertFalse(args.isValid());\n    assertEquals(0, args.cardinality());\n    assertFalse(args.has('x'));\n    assertEquals(0.0, args.getDouble('x'), 0.01);\n    assertEquals(\"Could not find double parameter for -x.\",\n                 args.errorMessage());\n  }"}, {"type": "para", "sentences": [{"en": "This passes as expected. We wrote it simply for completeness.", "tr": "Beklendiği gibi geçiyor. Bunu yalnızca tamamlılık (completeness) için yazdık."}]}, {"type": "para", "sentences": [{"en": "The exception code is pretty ugly and doesn’t really belong in the `Args` class.", "tr": "İstisna kodu oldukça çirkin ve aslında `Args` sınıfına ait değil."}, {"en": "We are also throwing out `ParseException`, which doesn’t really belong to us.", "tr": "Ayrıca dışarı attığımız `ParseException`, aslında bize ait değil."}, {"en": "So let’s merge all the exceptions into a single `ArgsException` class and move it into its own module.", "tr": "Bu yüzden tüm istisnaları tek bir `ArgsException` sınıfında birleştirip onu kendi modülüne taşıyalım."}]}, {"type": "code", "lang": "java", "code": "public class ArgsException extends Exception {\n  private char errorArgumentId = '\\0';\n  private String errorParameter = \"TILT\";\n  private ErrorCode errorCode = ErrorCode.OK;\n\n  public ArgsException() {}\n\n  public ArgsException(String message) {super(message);}\n\n  public enum ErrorCode {\n    OK, MISSING_STRING, MISSING_INTEGER, INVALID_INTEGER, UNEXPECTED_ARGUMENT,\n    MISSING_DOUBLE, INVALID_DOUBLE}\n}\n---"}], "concepts": [{"id": "test-every-error-message", "title": {"en": "Test Every Error Message", "tr": "Her Hata Mesajını Test Et"}, "summary": {"en": "An error message is part of the public contract of a parser: the user reads it, the support engineer searches for it. If tests only exercise the happy path, a typo or rewording in any error message ships silently. Each error code should have a test that asserts its exact message.", "tr": "Bir hata mesajı bir ayrıştırıcının (parser) kamusal sözleşmesinin bir parçasıdır: kullanıcı onu okur, destek mühendisi ondan arama yapar. Testler yalnızca mutlu yolu (happy path) çalıştırıyorsa, bir hata mesajındaki yazım hatası ya da yeniden ifadeleme sessizce üretime (production) ulaşır. Her hata kodunun (error code), mesajının tam metnini doğrulayan (assert) bir testi olmalıdır."}, "bad": {"lang": "python", "code": "def test_valid_age():\n    p = Person(\"42\")\n    assert p.age() == 42", "why": {"en": "Only the happy path is verified. If the 'not a number' message regresses to an empty string or a raw stack trace, every test stays green.", "tr": "Yalnızca mutlu yol (happy path) doğrulanıyor. 'Sayı değil' mesajı boş bir dizgiye ya da ham bir yığın izine (stack trace) bozulsa bile tüm testler yeşil kalır."}}, "good": {"lang": "python", "code": "def test_not_a_number_message():\n    with pytest.raises(ParseError) as exc:\n        Person(\"forty two\")\n    assert str(exc.value) == \"Age 'forty two' is not a number.\"\n\ndef test_missing_age_message():\n    with pytest.raises(ParseError) as exc:\n        Person(None)\n    assert str(exc.value) == \"Missing age argument.\"", "why": {"en": "Each error code gets its own test asserting the exact message, punctuation included. Any rewording fails the build immediately and shows which message changed.", "tr": "Her hata kodunun, nokta dahil tam mesajını doğrulayan kendi testi vardır. Herhangi bir yeniden ifadeleme derlemeyi anında düşürür ve hangi mesajın değiştiğini gösterir."}}, "tip": {"en": "Treat error messages like a public API: write one test per code that asserts the full message, and review rewordings as breaking changes.", "tr": "Hata mesajlarını bir kamusal API gibi değerlendirin: her kod için tam mesajı doğrulayan bir test yazın ve yeniden ifadeleri kırıcı değişiklik (breaking change) gibi gözden geçirin."}}, {"id": "split-the-god-exception", "title": {"en": "Split the God Exception", "tr": "Tanrı İstisnasını Böl"}, "summary": {"en": "An exception that both remembers which error happened and renders the human-readable message grows one more switch case per new failure. Move the exception into its own module, let it carry only the error state (code plus context), and leave message rendering to the caller or a dedicated formatter.", "tr": "Hem hangi hatanın olduğunu hatırlayan hem de insan-okunur mesajı üreten bir istisna, her yeni hata durumunda bir switch dalı daha büyür. İstisnayla birlikte hata durumunu (kodu ve bağlamı) kendi modülüne taşıyın; mesaj üretimi çağıran tarafın ya da ayrı bir biçimlendiricinin (formatter) sorumluluğunda kalsın."}, "bad": {"lang": "java", "code": "public class ReportException extends Exception {\n    private String fileName = \"TILT\";\n    private int line = -1;\n    private Kind kind = Kind.NONE;\n\n    public enum Kind { NONE, BAD_DATE, MISSING_FILE, BAD_AMOUNT }\n\n    public String render() {\n        switch (kind) {\n            case BAD_DATE:     return \"Bad date on line \" + line + \" in \" + fileName;\n            case MISSING_FILE: return \"File not found: \" + fileName;\n            case BAD_AMOUNT:   return \"Bad amount on line \" + line;\n            default:           return \"TILT\";\n        }\n    }\n}", "why": {"en": "The exception couples error state with presentation: every new failure type adds a branch to render(), and i18n or log-format changes force editing the exception class itself.", "tr": "İstisna hata durumunu sunumla (presentation) bağlar: her yeni hata türü render() içine yeni bir dal ekler, çeviri (i18n) ya da günlük biçimi değişiklikleri ise istisna sınıfının kendisinin düzenlenmesini zorlar."}}, "good": {"lang": "java", "code": "// errors/ReportException.java — kendi modülünde, yalnızca durumu taşır\npublic class ReportException extends Exception {\n    public enum Kind { NONE, BAD_DATE, MISSING_FILE, BAD_AMOUNT }\n    private final Kind kind;\n    private final int line;\n\n    public ReportException(Kind kind, String file, int line) {\n        super(file + \":\" + line);\n        this.kind = kind;\n        this.line = line;\n    }\n    public Kind kind() { return kind; }\n    public int line() { return line; }\n}", "why": {"en": "The exception carries only the error state (kind plus line) and lives in its own module; message rendering belongs to the layer that displays or logs it, so changing either side does not touch the other.", "tr": "İstisna yalnızca hata durumunu (kind ve line) taşır ve kendi modülünde yaşar; mesaj üretimi onu ekrana yazan ya da günlüğe alan katmana aittir, böylece bir tarafı değiştirmek diğerine dokunmaz."}}, "tip": {"en": "If your exception class has a method that builds strings from its fields, that is a smell: the class is doing presentation. Split state from rendering, and give the exception its own file or package.", "tr": "İstisna sınıfınızda alanlarından dizgi kuran bir metot varsa bu bir kokudur (code smell): sınıf sunum (presentation) yapıyordur. Durumu ve üretimi ayırın; istisnayı kendi dosyasına ya da paketlerine taşıyın."}}]});
