window.PAGE({"id": "page-216", "page": 216, "pdf_page": 216, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Integer Argument Marshalling", "tr": "Tamsayı Argümanı Marshalling'i"}, "blocks": [{"type": "code", "lang": "java", "code": "  private void parseIntegerSchemaElement(char elementId) {\n    intArgs.put(elementId, new IntegerArgumentMarshaler());\n  }\n...\n  private void setIntArg(char argChar) throws ArgsException {\n    currentArgument++;\n    String parameter = null;\n    try {\n      parameter = args[currentArgument];\n      intArgs.get(argChar).setInteger(Integer.parseInt(parameter));\n    } catch (ArrayIndexOutOfBoundsException e) {\n      valid = false;\n      errorArgumentId = argChar;\n      errorCode = ErrorCode.MISSING_INTEGER;\n      throw new ArgsException();\n    } catch (NumberFormatException e) {\n      valid = false;\n      errorArgumentId = argChar;\n      errorParameter = parameter;\n      errorCode = ErrorCode.INVALID_INTEGER;\n      throw new ArgsException();\n    }\n  }\n...\n  public int getInt(char arg) {\n    Args.ArgumentMarshaler am = intArgs.get(arg);\n    return am == null ? 0 : am.getInteger();\n  }\n...\n  private class ArgumentMarshaler {\n    private boolean booleanValue = false;\n    private String stringValue;\n    private int integerValue;\n\n    public void setBoolean(boolean value) {\n      booleanValue = value;\n    }\n\n    public boolean getBoolean() {\n      return booleanValue;\n    }\n\n    public void setString(String s) {\n      stringValue = s;\n    }\n\n    public String getString() {\n      return stringValue == null ? \"\" : stringValue;\n    }\n\n    public void setInteger(int i) {\n      integerValue = i;\n    }\n\n    public int getInteger() {\n      return integerValue;\n    }\n  }"}], "concepts": [{"id": "int-parse-in-marshaler", "title": {"en": "Int Parse in the Marshaler", "tr": "Marshaler'de Tamsayı Ayrıştırma"}, "summary": {"en": "Parsing a string into an int belongs in exactly one place: the marshaler that owns that argument type. When every call site does its own Integer.parseInt, the same try/catch and error-reporting logic is copy-pasted across the codebase.", "tr": "Bir dizgiyi int'e çevirmek tam olarak bir yere ait olmalı: o argüman türüne sahip olan marshaler'e. Her çağrı noktası kendi Integer.parseInt'ini yaptığında, aynı try/catch ve hata bildirme mantığı tüm koda kopyala-yapıştır ile yayılır."}, "bad": {"lang": "java", "code": "public void configure(String[] args) {\n  for (int i = 0; i < args.length; i++) {\n    if (\"-port\".equals(args[i]))\n      port = Integer.parseInt(args[++i]);\n    if (\"-timeout\".equals(args[i]))\n      timeout = Integer.parseInt(args[++i]);\n    if (\"-retries\".equals(args[i]))\n      retries = Integer.parseInt(args[++i]);\n  }\n}", "why": {"en": "Every flag repeats the same parse call. A bad value like 'abc' throws an uncaught NumberFormatException and the user cannot tell which flag was wrong. Each new int flag adds another copy of the same code.", "tr": "Her bayrak aynı parse çağrısını tekrarlar. 'abc' gibi hatalı bir değer yakalanmamış bir NumberFormatException fırlatır ve kullanıcı hangi bayrağın yanlış olduğunu anlayamaz. Her yeni int bayrağı aynı kodun bir kopyasını daha ekler."}}, "good": {"lang": "java", "code": "private final Map<Character, IntOption> intArgs =\n    new HashMap<Character, IntOption>();\n\nprivate void setIntArg(char argChar, String raw) {\n  try {\n    intArgs.get(argChar).value = Integer.parseInt(raw);\n  } catch (NumberFormatException e) {\n    errorArgumentId = argChar;\n    errorParameter = raw;\n    errorCode = ErrorCode.INVALID_INTEGER;\n    throw new ArgsException();\n  }\n}", "why": {"en": "Parsing lives in one method. Adding a new int flag only needs a new entry in the map: no new parse code, no duplicated error handling.", "tr": "Ayrıştırma tek metotta yaşar. Yeni bir int bayrağı eklemek haritada yalnızca yeni bir kayıt ister: yeni parse kodu, tekrarlanan hata yönetimi yok."}}, "tip": {"en": "Rule of thumb: if two call sites parse the same raw input into the same type, extract the parse into the class that owns that type (the marshaler) and keep the call sites one line.", "tr": "Kural: aynı ham girdiyi aynı türe çeviren iki çağrı noktası varsa, ayrıştırmayı o türe sahip sınıfa (marshaler'e) çıkarın ve çağrı noktalarını tek satırda tutun."}}, {"id": "error-code-on-parse-fail", "title": {"en": "Error Code on Parse Fail", "tr": "Ayrıştırmada Hata Kodu"}, "summary": {"en": "Rethrowing a raw NumberFormatException hides which argument failed and what type was expected. Structured error fields — which flag, which raw value, which error code — let the caller build a precise, user-facing message.", "tr": "Ham bir NumberFormatException'i yeniden fırlatmak, hangi argümanın başarısız olduğunu ve hangi tipin beklendiğini gizler. Yapılı hata alanları — hangi bayrak, hangi ham değer, hangi hata kodu — çağıranın kesin, kullanıcıya dönük bir mesaj kurmasını sağlar."}, "bad": {"lang": "java", "code": "public int getPort(String[] args) {\n  for (int i = 0; i < args.length; i++)\n    if (\"-port\".equals(args[i]))\n      return Integer.parseInt(args[i + 1]);\n  return 8080;\n}\n// hata durumunda kullanıcı görür: \"For input string: abc\"\n// hangi argüman? hangi bayrak? — bilinemiyor", "why": {"en": "The raw exception message reveals only the offending string. The caller has no argument id or expected type, so it cannot produce a message like 'invalid port: abc'.", "tr": "Ham istisna mesajı yalnızca sorunlu dizgiyi gösterir. Çağıranın argüman kimliği ya da beklenen tipi olmadığı için 'geçersiz port: abc' gibi bir mesaj üretemez."}}, "good": {"lang": "java", "code": "public enum ErrorCode { MISSING_INTEGER, INVALID_INTEGER }\n\nprivate char errorArgumentId;\nprivate String errorParameter;\nprivate ErrorCode errorCode;\n\nprivate void fail(char arg, String param, ErrorCode code) {\n  errorArgumentId = arg;\n  errorParameter = param;\n  errorCode = code;\n}\n// hata: -p argümanı için 'abc' -> INVALID_INTEGER\n// mesaj: \"Invalid integer for argument -p: abc\"", "why": {"en": "Structured fields (argument id, raw value, error code) let the caller generate an exact, translatable message without parsing exception text.", "tr": "Yapılı alanlar (argüman kimliği, ham değer, hata kodu) çağıranın istisna metnini çözümlenmeden tam ve çevrilebilir bir mesaj üretmesini sağlar."}}, "tip": {"en": "Pair every error code with the identity of the offending argument. 'INVALID_INTEGER' alone tells a developer what broke but never where; argument id plus raw value plus code is a complete diagnosis.", "tr": "Her hata kodunu, sorunlu argümanın kimliğiyle eşleyin. Tek başına 'INVALID_INTEGER' neyin bozulduğunu söyler ama nerede söylediği sorusuna yanıt vermez; argüman kimliği + ham değer + kod tam bir tanıdır."}}, {"id": "store-parsed-value-not-raw-string", "title": {"en": "Store the Parsed Value, Not the Raw String", "tr": "Ayrıştırılmış Değeri Sakla, Ham Dizgiyi Değil"}, "summary": {"en": "A marshalled value should be stored in its target type. Keeping the raw string and re-parsing it in every getter defers the failure to read time and makes getters secretly fragile.", "tr": "Marshalling edilmiş bir değer hedef türünde saklanmalıdır. Ham dizgiyi tutup her getter'da yeniden ayırtmak, hatayı okuma zamanına erteler ve getter'ları gizlice kırılgan yapar."}, "bad": {"lang": "java", "code": "class Options {\n  private String port;\n\n  void setPort(String raw) {\n    this.port = raw;\n  }\n\n  int getPort() {\n    return Integer.parseInt(port); // her okumada parse\n  }\n}", "why": {"en": "Every call to getPort can throw: the failure mode moves from a controlled write to an unexpected read, and the same string is re-parsed on every access.", "tr": "getPort çağrılarının her biri istisna fırlatabilir: hata modu kontrollü bir yazımdan beklenmedik bir okumaya kayar ve aynı dizgi her erişimde yeniden ayrıştırılır."}}, "good": {"lang": "java", "code": "class IntOption {\n  private int value = 0;\n\n  void set(String raw) throws ArgsException {\n    try {\n      value = Integer.parseInt(raw);\n    } catch (NumberFormatException e) {\n      throw new ArgsException(ErrorCode.INVALID_INTEGER, raw);\n    }\n  }\n\n  int get() {\n    return value; // okuma asla parse etmez\n  }\n}", "why": {"en": "Parsing happens exactly once, at the boundary, when the raw value arrives. The getter returns a typed value with no failure mode.", "tr": "Ayrıştırma, ham değerin geldiği anda, sınırda tam olarak bir kez yapılır. Getter, hata modu olmayan tip'li bir değer döndürür."}}, "tip": {"en": "Convert at the boundary, use the typed value everywhere after. If a getter has to convert its own field, the conversion belongs in the setter (or the constructor) instead.", "tr": "Dönüşümü sınırda yapın, sonrasında her yerde tip'li değeri kullanın. Bir getter kendi alanını dönüştürmek zorundaysa, dönüşüm setter'da (ya da kurucuda) olmalıdır."}}]});
