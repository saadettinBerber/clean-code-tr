window.PAGE({"id": "page-73", "page": 73, "pdf_page": 104, "chapter": {"num": 4, "en": "Comments", "tr": "Yorumlar"}, "section": {"en": "PrimeGenerator (refactored) — Listing 4-8 continued", "tr": "PrimeGenerator (yeniden düzenlenmiş) — Listing 4-8 devamı"}, "title": {"en": "Listing 4-8 PrimeGenerator (refactored) continued", "tr": "Listing 4-8 PrimeGenerator (yeniden düzenlenmiş) devamı"}, "blocks": [{"type": "caption", "kind": "listing", "en": "Listing 4-8 (continued) PrimeGenerator.java (refactored)", "tr": "Listing 4-8 (devamı) PrimeGenerator.java (yeniden düzenlenmiş)"}, {"type": "code", "lang": "java", "code": " * multiples.  Repeat until there are no more multiples\n * in the array.\n */\n\npublic class PrimeGenerator\n{\n  private static boolean[] crossedOut;\n  private static int[] result;\n\n  public static int[] generatePrimes(int maxValue)\n  {\n    if (maxValue < 2)\n      return new int[0];\n    else\n    {\n      uncrossIntegersUpTo(maxValue);\n      crossOutMultiples();\n      putUncrossedIntegersIntoResult();\n      return result;\n    }\n  }\n\n  private static void uncrossIntegersUpTo(int maxValue)\n  {\n    crossedOut = new boolean[maxValue + 1];\n    for (int i = 2; i < crossedOut.length; i++)\n      crossedOut[i] = false;\n  }\n\n  private static void crossOutMultiples()\n  {\n    int limit = determineIterationLimit();\n    for (int i = 2; i <= limit; i++)\n      if (notCrossed(i))\n        crossOutMultiplesOf(i);\n  }\n\n  private static int determineIterationLimit()\n  {\n    // Every multiple in the array has a prime factor that\n    // is less than or equal to the root of the array size,\n    // so we don't have to cross out multiples of numbers\n    // larger than that root.\n    double iterationLimit = Math.sqrt(crossedOut.length);\n    return (int) iterationLimit;\n  }\n\n  private static void crossOutMultiplesOf(int i)\n  {\n    for (int multiple = 2*i;\n         multiple < crossedOut.length;\n         multiple += i)\n      crossedOut[multiple] = true;\n  }"}], "concepts": [{"id": "prime-generator-structure", "title": {"en": "PrimeGenerator Structure", "tr": "PrimeGenerator Yapısı"}, "summary": {"en": "The refactored PrimeGenerator splits the sieve into intention-revealing steps: uncross up to max, cross multiples, collect results.", "tr": "Yeniden düzenlenmiş PrimeGenerator, kalburu niyet belirten adımlara böler: max'e kadar işaretleri kaldır, katları ele, sonuçları topla."}, "bad": {"lang": "java", "code": "for (i = 2; i < Math.sqrt(s) + 1; i++) if (f[i]) for (j = 2*i; j < s; j+=i) f[j]=false;", "why": {"en": "Single nested loop with cryptic variables hides the sieve steps.", "tr": "Şifreli değişkenlere sahip tek bir iç içe döngü kalbur adımlarını gizler."}}, "good": {"lang": "java", "code": "uncrossIntegersUpTo(maxValue);\ncrossOutMultiples();\nputUncrossedIntegersIntoResult();", "why": {"en": "Three well-named calls make the algorithm read like prose; no comment needed.", "tr": "İyi adlandırılmış üç çağrı algoritmayı düzyazı gibi okutur; yoruma gerek kalmaz."}}, "tip": {"en": "Extract till each function is one abstraction level and its name is the comment.", "tr": "Her fonksiyon tek bir soyutlama seviyesi (abstraction level) olana ve ismi yorumun kendisi olana kadar parçalara ayırın."}}]});
