window.PAGE({"id": "page-347", "page": 347, "pdf_page": 347, "chapter": {"num": 18, "en": "Appendices", "tr": "Ekler"}, "section": {"en": "Tutorial: Full Code Examples", "tr": "Öğretici: Tam Kod Örnekleri (Tutorial: Full Code Examples)"}, "title": {"en": "Listing A-4 (continued) ClientTest.java", "tr": "Listing A-4 (devamı) ClientTest.java"}, "blocks": [{"type": "code", "lang": "java", "code": "            try {\n                System.out.printf(\"Server: getting message\\n\");\n                String message = MessageUtils.getMessage(socket);\n                System.out.printf(\"Server: got message: %s\\n\", message);\n                Thread.sleep(1000);\n                System.out.printf(\"Server: sending reply: %s\\n\", message);\n                MessageUtils.sendMessage(socket, \"Processed: \" + message);\n                System.out.printf(\"Server: sent\\n\");\n                closeIgnoringException(socket);\n            } catch (Exception e) {\n                e.printStackTrace();\n            }\n        }\n    };\n\n    Thread clientConnection = new Thread(clientHandler);\n    clientConnection.start();\n}"}], "concepts": [{"id": "thread-per-connection", "kind": "code", "title": {"en": "Thread per Connection", "tr": "Bağlantı Başına İş Parçacığı (Thread per Connection)"}, "summary": {"en": "The threaded server wraps each client's handling in a Runnable and hands it to a new Thread, so accept() is free to take the next connection while earlier clients are still being processed.", "tr": "İş parçacıklı sunucu, her istemcinin işlenmesini bir Runnable içine sarar ve bunu yeni bir Thread'e verir; böylece önceki istemciler hâlâ işlenirken accept() metotu bir sonraki bağlantıyı almakta serbest kalır."}, "bad": {"lang": "python", "code": "while True:\n    conn, _ = server_socket.accept()\n    process(conn)          # 1 sn'lik sleep, tüm sunucuyu bloklar\n    conn.close()", "why": {"en": "One slow client stalls every other pending connection; the accept loop cannot run until the current client is fully handled.", "tr": "Tek bir yavaş istemci, bekleyen diğer tüm bağlantıları durdurur; accept döngüsü, mevcut istemci tamamen işlenmeden çalışamaz."}}, "good": {"lang": "java", "code": "Runnable clientHandler = () -> {\n    try {\n        String message = MessageUtils.getMessage(socket);\n        Thread.sleep(1000);\n        MessageUtils.sendMessage(socket, \"Processed: \" + message);\n    } catch (Exception e) {\n        e.printStackTrace();\n    } finally {\n        closeIgnoringException(socket);\n    }\n};\nnew Thread(clientHandler).start();", "why": {"en": "Each client gets its own thread, so the accept loop immediately returns to waiting for new connections while earlier clients are handled concurrently.", "tr": "Her istemci kendi iş parçacığına sahip olur; böylece accept döngüsü, önceki istemciler eşzamanlı (concurrently) işlenirken hemen yeni bağlantıları beklemeye döner."}}, "tip": {"en": "Threads add concurrency but also shared-state risks: guard any shared data with locks or use thread-safe collections, and prefer thread pools for many connections.", "tr": "İş parçacıkları eşzamanlılık katar ama aynı zamanda paylaşılan durum (shared state) riskleri getirir: paylaşılan verileri kilitlerle koruyun ya da iş parçacığına güvenli (thread-safe) koleksiyonlar kullanın; çok sayıda bağlantı için iş parçacığı havuzlarını (thread pools) tercih edin."}}, {"id": "anonymous-inner-class-runnable", "kind": "code", "title": {"en": "Anonymous Inner Class as a Runnable", "tr": "Anonim İç Sınıf (Anonymous Inner Class) Runnable"}, "summary": {"en": "The code declares an anonymous inner class that implements Runnable and defines run(); Java's final (effectively final) rule applies to every local variable it captures.", "tr": "Kod, Runnable arayüzünü gerçekleştiren ve run() metotunu tanımlayan bir anonim iç sınıf (anonymous inner class) bildirir; yakaladığı her yerel değişken için Java'nın final (fiilen final) kuralı geçerlidir."}, "bad": {"lang": "java", "code": "int counter = 0;\nRunnable r = new Runnable() {\n    public void run() {\n        counter++;   // derleme hatası: final ya da fiilen final değil\n    }\n};", "why": {"en": "The compiler forbids capturing a mutable local variable inside an anonymous inner class; the variable must be final or effectively final.", "tr": "Derleyici, anonim iç sınıf (anonymous inner class) içinde değiştirilebilir bir yerel değişkenin yakalanmasını yasaklar; değişken final ya da fiilen final (effectively final) olmalıdır."}}, "good": {"lang": "java", "code": "Runnable clientHandler = new Runnable() {\n    public void run() {\n        process(socket);   // socket fiilen final olduğu için güvenle yakalanır\n    }\n};\nThread clientConnection = new Thread(clientHandler);\nclientConnection.start();", "why": {"en": "The socket parameter is effectively final, so the anonymous class can safely capture it, and the worker runs on its own thread.", "tr": "socket parametresi fiilen final (effectively final) olduğundan anonim sınıf onu güvenle yakalayabilir ve çalışan kendi iş parçacığında koşar."}}, "tip": {"en": "Prefer a lambda or a named Runnable inner class when the body grows; keep any captured state immutable.", "tr": "Gövde büyüdüğünde bir lambda ya da adlandırılmış bir Runnable iç sınıfını tercih edin; yakalanan durumu değiştirilemez (immutable) tutun."}}, {"id": "start-vs-run-thread", "kind": "code", "title": {"en": "Thread.start() vs. run()", "tr": "Thread.start() ile run() Farkı"}, "summary": {"en": "After building the Runnable, the server wraps it in a Thread and calls start(); run() then executes on its own thread, and accept() can keep taking connections.", "tr": "Runnable oluşturulduktan sonra sunucu onu bir Thread içine sarar ve start() çağırır; run() metotu artık kendi iş parçacığında yürür ve accept() yeni bağlantılar almaya devam edebilir."}, "bad": {"lang": "java", "code": "clientHandler.run();        // geçerli iş parçacığında çalışır\nacceptNextClient();          // handler bitene dek bloklanır", "why": {"en": "Calling run() directly executes the body on the current thread, so the server still blocks on the client's work and gains no concurrency.", "tr": "run() metotunu doğrudan çağırmak gövdeyi geçerli iş parçacığında çalıştırır; böylece sunucu hâlâ istemcinin işinde bloklanır ve hiçbir eşzamanlılık kazanılmaz."}}, "good": {"lang": "java", "code": "Thread clientConnection = new Thread(clientHandler);\nclientConnection.start();   // run()'ı yeni bir iş parçacığında zamanlar\nacceptNextClient();         // hemen diğer istemcileri kabul etmeye serbest", "why": {"en": "start() creates a new execution context in which run() runs, so the calling code never blocks on the client's work.", "tr": "start() metotu, run()'ın koştuğu yeni bir yürütme bağlamı oluşturur; böylece çağıran kod asla istemcinin işinde bloklanmaz."}}, "tip": {"en": "For many connections, use an executor framework or thread pools instead of creating a raw Thread per request; unbounded thread creation exhausts resources.", "tr": "Çok sayıda bağlantı için, istek başına ham bir Thread oluşturmak yerine bir İcracı Çerçevesi (Executor Framework) ya da iş parçacığı havuzları (thread pools) kullanın; sınırsız iş parçacığı oluşturmak kaynakları tüketir."}}]});
