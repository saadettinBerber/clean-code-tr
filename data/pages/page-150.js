window.PAGE({"id": "page-150", "page": 150, "pdf_page": 150, "chapter": {"num": 10, "en": "Classes", "tr": "Sınıflar"}, "section": {"en": "Isolating from Change", "tr": "Değişimden Yalıtma"}, "title": {"en": "Depending on Abstractions", "tr": "Soyutlamalara Bağımlı Olmak"}, "blocks": [{"type": "para", "sentences": [{"en": "We design `TokyoStockExchange` to implement this interface.", "tr": "`TokyoStockExchange` sınıfını bu arayüzü gerçekleştirecek şekilde tasarlarız."}, {"en": "We also make sure that the constructor of `Portfolio` takes a `StockExchange` reference as an argument:", "tr": "Ayrıca `Portfolio` sınıfının kurucusunun (constructor) argüman olarak bir `StockExchange` referansı almasını sağlarız:"}]}, {"type": "code", "lang": "java", "code": "public Portfolio {\n   private StockExchange exchange;\n   public Portfolio(StockExchange exchange) {\n      this.exchange = exchange;\n   }\n   // ...\n}"}, {"type": "para", "sentences": [{"en": "Now our test can create a testable implementation of the `StockExchange` interface that emulates the `TokyoStockExchange`.", "tr": "Artık testimiz, `StockExchange` arayüzünün `TokyoStockExchange` davranışını taklit eden, test edilebilir bir gerçekleştirimini (implementation) oluşturabilir."}, {"en": "This test implementation will fix the current value for any symbol we use in testing.", "tr": "Bu test gerçekleştirimi, testte kullandığımız her sembolün güncel değerini sabitler."}, {"en": "If our test demonstrates purchasing five shares of Microsoft for our portfolio, we code the test implementation to always return $100 per share of Microsoft.", "tr": "Testimiz portföyümüz için beş adet Microsoft hissesi satın alındığını gösteriyorsa, test gerçekleştirimini Microsoft'un hisse başına değeri olarak her zaman 100 dolar döndürecek şekilde kodlarız."}, {"en": "Our test implementation of the `StockExchange` interface reduces to a simple table lookup.", "tr": "Böylece `StockExchange` arayüzünün test gerçekleştirimi basit bir tablo aramasına (table lookup) indirgenir."}, {"en": "We can then write a test that expects $500 for our overall portfolio value.", "tr": "Ardından toplam portföy değerimiz için 500 dolar bekleyen bir test yazabiliriz."}]}, {"type": "code", "lang": "java", "code": "public class PortfolioTest {\n  private FixedStockExchangeStub exchange;\n  private Portfolio portfolio;\n\n  @Before\n  protected void setUp() throws Exception {\n    exchange = new FixedStockExchangeStub();\n    exchange.fix(\"MSFT\", 100);\n    portfolio = new Portfolio(exchange);\n  }\n\n  @Test\n  public void GivenFiveMSFTTotalShouldBe500() throws Exception {\n    portfolio.add(5, \"MSFT\");\n    Assert.assertEquals(500, portfolio.value());\n  }\n}"}, {"type": "para", "sentences": [{"en": "If a system is decoupled enough to be tested in this way, it will also be more flexible and promote more reuse.", "tr": "Bir sistem bu şekilde test edilebilecek kadar ayrıştırılmışsa (decoupled), aynı zamanda daha esnek olur ve daha fazla yeniden kullanımı teşvik eder."}, {"en": "The lack of coupling means that the elements of our system are better isolated from each other and from change.", "tr": "Bağımlılığın (coupling) olmaması, sistemimizin öğelerinin hem birbirinden hem de değişimden daha iyi yalıtıldığı anlamına gelir."}, {"en": "This isolation makes it easier to understand each element of the system.", "tr": "Bu yalıtım, sistemin her bir öğesini anlamayı kolaylaştırır."}]}, {"type": "para", "sentences": [{"en": "By minimizing coupling in this way, our classes adhere to another class design principle known as the Dependency Inversion Principle (DIP).5 In essence, the DIP says that our classes should depend upon abstractions, not on concrete details.", "tr": "Bağımlılığı bu şekilde en aza indirdiğimizde sınıflarımız, Bağımlılık Tersine Çevirme İlkesi (Dependency Inversion Principle, DIP) olarak bilinen bir başka sınıf tasarım ilkesine de uymuş olur.5 Özünde DIP, sınıflarımızın somut ayrıntılara değil, soyutlamalara bağımlı olması gerektiğini söyler."}]}, {"type": "para", "sentences": [{"en": "Instead of being dependent upon the implementation details of the `TokyoStockExchange` class, our `Portfolio` class is now dependent upon the `StockExchange` interface.", "tr": "`Portfolio` sınıfımız artık `TokyoStockExchange` sınıfının gerçekleştirim ayrıntılarına değil, `StockExchange` arayüzüne bağımlıdır."}, {"en": "The `StockExchange` interface represents the abstract concept of asking for the current price of a symbol.", "tr": "`StockExchange` arayüzü, bir sembolün güncel fiyatını sorma eyleminin soyut kavramını temsil eder."}, {"en": "This abstraction isolates all of the specific details of obtaining such a price, including from where that price is obtained.", "tr": "Bu soyutlama (abstraction), böyle bir fiyatı elde etmenin tüm özel ayrıntılarını, fiyatın nereden alındığı da dahil olmak üzere, yalıtır."}]}, {"type": "footnote", "en": "5. [PPP].", "tr": "5. [PPP]."}], "concepts": [{"id": "inject-abstraction-through-constructor", "kind": "code", "title": {"en": "Inject the Abstraction Through the Constructor", "tr": "Soyutlamayı Kurucu Üzerinden Enjekte Edin"}, "summary": {"en": "When a class builds its own concrete dependency inside the constructor, nothing can be swapped for testing. Accepting an interface as a constructor argument lets tests hand in a controllable implementation.", "tr": "Bir sınıf somut bağımlılığını kurucusunun içinde kendisi oluşturursa test için hiçbir şey değiştirilemez. Kurucunun argüman olarak bir arayüz kabul etmesi, testlerin denetlenebilir bir gerçekleştirim vermesine olanak tanır."}, "bad": {"lang": "python", "code": "class WeatherReport:\n    def __init__(self):\n        self.client = OpenWeatherHttpClient(api_key=\"...\")\n\n    def headline(self, city):\n        temp = self.client.fetch_temperature(city)\n        return f\"{city}: {temp} C\"", "why": {"en": "`WeatherReport` creates the HTTP client itself, so every test hits the real network and returns whatever the weather happens to be right now.", "tr": "`WeatherReport` HTTP istemcisini kendisi oluşturur; bu yüzden her test gerçek ağa gider ve o anki hava neyse onu döndürür."}}, "good": {"lang": "python", "code": "class WeatherReport:\n    def __init__(self, source: TemperatureSource):\n        self.source = source\n\n    def headline(self, city):\n        temp = self.source.temperature_of(city)\n        return f\"{city}: {temp} C\"\n\n\nclass FixedTemperatureSource(TemperatureSource):\n    def __init__(self, degrees):\n        self.degrees = degrees\n\n    def temperature_of(self, city):\n        return self.degrees\n\n\ndef test_headline_uses_source_temperature():\n    report = WeatherReport(FixedTemperatureSource(21))\n    assert report.headline(\"Ankara\") == \"Ankara: 21 C\"", "why": {"en": "The report depends only on the `TemperatureSource` abstraction. The test injects a fixed source and gets the same answer every time.", "tr": "Rapor yalnızca `TemperatureSource` soyutlamasına bağımlıdır. Test sabit bir kaynak enjekte eder ve her seferinde aynı yanıtı alır."}}, "tip": {"en": "If you cannot write a test without touching a network, clock, or database, the class is probably constructing its own dependency. Move that choice to the caller.", "tr": "Bir testi ağa, saate ya da veritabanına dokunmadan yazamıyorsanız sınıf muhtemelen bağımlılığını kendisi oluşturuyordur. Bu seçimi çağırana taşıyın."}}, {"id": "fixed-value-stub-for-volatile-data", "kind": "code", "title": {"en": "Fix Volatile Data with a Stub", "tr": "Değişken Veriyi Bir Stub ile Sabitleyin"}, "summary": {"en": "A stub that returns a fixed value for each key turns an unpredictable external lookup into a simple table. The test then knows exactly which result to expect.", "tr": "Her anahtar için sabit bir değer döndüren bir stub, öngörülemeyen bir dış aramayı basit bir tabloya dönüştürür. Test böylece tam olarak hangi sonucu bekleyeceğini bilir."}, "bad": {"lang": "javascript", "code": "test('converts 10 EUR to USD', async () => {\n  const converter = new CurrencyConverter(new LiveRateApi());\n  const usd = await converter.convert(10, 'EUR', 'USD');\n  expect(usd).toBeCloseTo(10.9);   // true today, false tomorrow\n});", "why": {"en": "The expected value depends on the live exchange rate, so the test fails whenever the market moves.", "tr": "Beklenen değer canlı döviz kuruna bağlıdır; bu yüzden piyasa her hareket ettiğinde test başarısız olur."}}, "good": {"lang": "javascript", "code": "class FixedRateStub {\n  constructor() { this.rates = new Map(); }\n  fix(pair, rate) { this.rates.set(pair, rate); }\n  async rateFor(from, to) { return this.rates.get(`${from}/${to}`); }\n}\n\ntest('converts 10 EUR to USD', async () => {\n  const rates = new FixedRateStub();\n  rates.fix('EUR/USD', 1.25);\n  const converter = new CurrencyConverter(rates);\n\n  const usd = await converter.convert(10, 'EUR', 'USD');\n\n  expect(usd).toBe(12.5);\n});", "why": {"en": "The stub reduces the rate provider to a table lookup. The test controls its inputs and the assertion is exact and repeatable.", "tr": "Stub, kur sağlayıcısını bir tablo aramasına indirger. Test girdilerini kendisi denetler; doğrulama kesin ve tekrarlanabilirdir."}}, "tip": {"en": "Give the stub a small `fix(key, value)` method like the book's `FixedStockExchangeStub`; it reads naturally inside `setUp`.", "tr": "Stub'a kitaptaki `FixedStockExchangeStub` gibi küçük bir `fix(key, value)` metodu verin; `setUp` içinde doğal okunur."}}, {"id": "dip-depend-on-abstractions", "kind": "code", "title": {"en": "Dependency Inversion Principle (DIP)", "tr": "Bağımlılık Tersine Çevirme İlkesi (DIP)"}, "summary": {"en": "High-level policy should not depend on low-level details. Both should depend on an abstraction, so the details can change without touching the policy.", "tr": "Üst seviye politika, alt seviye ayrıntılara bağımlı olmamalıdır. İkisi de bir soyutlamaya bağımlı olmalıdır; böylece ayrıntılar politikaya dokunmadan değişebilir."}, "bad": {"lang": "java", "code": "public class InvoiceService {\n  private final MySqlInvoiceRepository repository =\n      new MySqlInvoiceRepository(\"jdbc:mysql://prod/billing\");\n\n  public Money totalDueFor(Customer customer) {\n    Money total = Money.zero();\n    for (Invoice invoice : repository.findOpenInvoices(customer))\n      total = total.plus(invoice.amount());\n    return total;\n  }\n}", "why": {"en": "The business rule for totaling invoices is welded to MySQL. Switching storage, or testing without a database, means editing the service.", "tr": "Faturaları toplama iş kuralı MySQL'e kaynaklanmıştır. Depolamayı değiştirmek ya da veritabanı olmadan test etmek, servisi düzenlemeyi gerektirir."}}, "good": {"lang": "java", "code": "public interface InvoiceRepository {\n  List<Invoice> findOpenInvoices(Customer customer);\n}\n\npublic class InvoiceService {\n  private final InvoiceRepository repository;\n\n  public InvoiceService(InvoiceRepository repository) {\n    this.repository = repository;\n  }\n\n  public Money totalDueFor(Customer customer) {\n    Money total = Money.zero();\n    for (Invoice invoice : repository.findOpenInvoices(customer))\n      total = total.plus(invoice.amount());\n    return total;\n  }\n}\n\n// MySqlInvoiceRepository implements InvoiceRepository\n// InMemoryInvoiceRepository implements InvoiceRepository (for tests)", "why": {"en": "`InvoiceService` now depends on the `InvoiceRepository` abstraction. MySQL and the in-memory test version are interchangeable details.", "tr": "`InvoiceService` artık `InvoiceRepository` soyutlamasına bağımlıdır. MySQL ve bellek içi test sürümü birbirinin yerine geçebilen ayrıntılardır."}}, "tip": {"en": "Name the interface after the concept the caller needs (a repository, a price source), not after the technology behind it.", "tr": "Arayüzü arkasındaki teknolojiye göre değil, çağıranın ihtiyaç duyduğu kavrama göre adlandırın (bir depo, bir fiyat kaynağı)."}}]});
