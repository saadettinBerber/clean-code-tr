window.PAGE({"id": "page-237", "page": 237, "pdf_page": 237, "chapter": {"num": 14, "en": "Successive Refinement", "tr": "Ardışık İyileştirme"}, "section": {"en": "String Arguments", "tr": "Dizgi Argümanları"}, "title": {"en": "Cleaning Up Integer Marshaler & Interface", "tr": "Integer Marshaler Temizliği ve Interface'e Dönüş"}, "blocks": [{"type": "para", "sentences": [{"en": "Now we can get rid of some crufty functions in `IntegerArgumentMarshaler` and clean it up a bit.", "tr": "Şimdi IntegerArgumentMarshaler'daki bazı paslı (crufty) fonksiyonlardan kurtulup onu biraz temizlayabiliriz."}]}, {"type": "code", "lang": "java", "code": "  private class IntegerArgumentMarshaler extends ArgumentMarshaler {\n    private int intValue = 0\n\n    public void set(Iterator<String> currentArgument) throws ArgsException {\n      String parameter = null;\n      try {\n        parameter = currentArgument.next();\n       intValue = Integer.parseInt(parameter);\n      } catch (NoSuchElementException e) {\n        errorCode = ErrorCode.MISSING_INTEGER;\n        throw new ArgsException();\n      } catch (NumberFormatException e) {\n        errorParameter = parameter;\n        errorCode = ErrorCode.INVALID_INTEGER;\n        throw new ArgsException();\n      }\n    }\n\n    public Object get() {\n      return intValue;\n    }\n  }"}, {"type": "para", "sentences": [{"en": "We can also turn `ArgumentMarshaler` into an interface.", "tr": "Ayrıca ArgumentMarshaler'ı bir arayüz (interface) haline getirebiliriz."}]}, {"type": "code", "lang": "java", "code": "  privateinterface ArgumentMarshaler {\n    void set(Iterator<String> currentArgument) throws ArgsException;\n    Object get();\n  }"}, {"type": "para", "sentences": [{"en": "So now let’s see how easy it is to add a new argument type to our structure.", "tr": "O halde şimdi, yapımıza yeni bir argüman tipi eklemenin ne kadar kolay olduğunu görelim."}, {"en": "It should require very few changes, and those changes should be isolated. First, we begin by adding a new test case to check that the double argument works correctly.", "tr": "Çok az değişiklik gerektirmelidir ve bu değişiklikler yalıtık (izole) olmalıdır. Önce, double argümanın doğru çalıştığını denetlemek için yeni bir test durumu (test case) ekleyerek başlıyoruz."}]}, {"type": "code", "lang": "java", "code": "  public void testSimpleDoublePresent() throws Exception {\n    Args args = new Args(\"x##\", new String[] {\"-x\",\"42.3\"});\n    assertTrue(args.isValid());\n    assertEquals(1, args.cardinality());\n    assertTrue(args.has('x'));\n    assertEquals(42.3, args.getDouble('x'), .001);\n  }"}, {"type": "para", "sentences": [{"en": "Now we clean up the schema parsing code and add the `##` detection for the double argument type.", "tr": "Şimdi şema ayrıştırma (schema parsing) kodunu temizleyip double argüman tipi için ## algılamasını ekliyoruz."}]}, {"type": "code", "lang": "java", "code": "  private void parseSchemaElement(String element) throws ParseException {\n    char elementId = element.charAt(0);\n    String elementTail = element.substring(1);\n    validateSchemaElementId(elementId);\n    if (elementTail.length() == 0)\n      marshalers.put(elementId, new BooleanArgumentMarshaler());\n    else if (elementTail.equals(\"*\"))\n      marshalers.put(elementId, new StringArgumentMarshaler());\n    else if (elementTail.equals(\"#\"))\n      marshalers.put(elementId, new IntegerArgumentMarshaler());"}], "concepts": [{"id": "class-to-interface", "kind": "code", "title": {"en": "Class to Interface", "tr": "Sınıftan Arayüze Dönüş"}, "summary": {"en": "When a base class no longer carries real shared behavior, turn it into a pure interface so that implementers depend on a contract, not on a concrete superclass.", "tr": "Bir üst sınıf artık gerçek bir ortak davranış taşımıyorsa onu saf bir arayüze (interface) dönüştürün; böylece gerçekleştirimler somut bir üst sınıfa değil, bir sözleşmeye bağımlı olur."}, "bad": {"lang": "java", "code": "abstract class Marshaler {\n  public Object validate() { return null; } // ortak davranış artık kalmadı\n}\n\nclass IntMarshaler extends Marshaler {\n  public Object parse(String s) { return Integer.parseInt(s); }\n}", "why": {"en": "The base class no longer carries shared behavior — validate() is a dead stub — yet it still forces a concrete superclass on every implementer; nobody can implement Marshaler without inheriting dead code.", "tr": "Üst sınıf artık ortak davranış taşımıyor — validate() ölü bir gövdedir — ama yine de her gerçekleştirimciye somut bir üst sınıf dayatıyor; ölü kodsuz Marshaler gerçekleştirmek mümkün değil."}}, "good": {"lang": "java", "code": "interface Marshaler {\n  Object parse(String s);\n}\n\nclass IntMarshaler implements Marshaler {\n  public Object parse(String s) { return Integer.parseInt(s); }\n}", "why": {"en": "A pure contract: implementers only promise what they can do; new types can be added without touching any shared code.", "tr": "Saf sözleşme: gerçekleştirimciler yalnızca yapabildiklerini söz verir; yeni tipler hiçbir ortak koda dokunulmadan eklenebilir."}}, "tip": {"en": "If the methods of an abstract class become empty or one-liners that are no longer shared, it is a signal to refactor it into an interface; when real shared code reappears, the reverse move (interface to abstract class) is equally valid.", "tr": "Bir soyut sınıftaki metotlar artık paylaşılmayan boş veya tek satırlık gövdere düşüyorsa, onu arayüze dönüştürmenin sinyalidir; gerçek ortak kod geri belirdiğinde tersi hamle (arayüzden soyut sınıfa) de eşit ölçüde geçerlidir."}}, {"id": "test-first-new-type", "kind": "code", "title": {"en": "Test-First New Type", "tr": "Test-Önceli Yeni Tip"}, "summary": {"en": "When adding a new feature (such as a new value type), write a failing test that fixes the expected contract first; then make the smallest production change that turns the test green.", "tr": "Yeni bir özellik (ör. yeni bir değer tipi) eklerken, önce beklenen sözleşmeyi sabitleyen başarısız bir test yazın; ardından testi yeşile çevirecek en küçük üretim kodu değişikliğini yapın."}, "bad": {"lang": "python", "code": "def load_config(pairs):\n    out = {}\n    for key, raw in pairs:\n        try:\n            out[key] = int(raw)\n        except ValueError:\n            out[key] = raw  # \"hmm, floats will also work this way\"\n    return out\n# no test: the float behavior is a side effect of the except branch", "why": {"en": "The new numeric type is smuggled in as an except-branch side effect; no test pins its behavior, so nothing fails when it later breaks.", "tr": "Yeni sayısal tip, bir except dalının yan etkisi olarak sızdırılır; davranışını sabitleyen bir test olmadığı için sonradan bozulduğunda hiçbir şey aksamaz."}}, "good": {"lang": "python", "code": "# 1) first the failing test: define the new type's contract\ndef test_float_value():\n    assert load_config([(\"ratio\", \"42.3\")])[\"ratio\"] == 42.3\n\n# 2) only then the minimal change\ndef load_config(pairs):\n    out = {}\n    for key, raw in pairs:\n        for cast in (int, float):\n            try:\n                out[key] = cast(raw)\n                break\n            except ValueError:\n                continue\n        else:\n            out[key] = raw\n    return out", "why": {"en": "The test fixes the expected behavior of the new type before any implementation exists; the change stays small and local, and the test turns red to green on the spot.", "tr": "Test, herhangi bir gerçekleştirim var olmadan önce yeni tipin beklenen davranışını sabitler; değişiklik küçük ve yerel kalır, test yerinde kırmızıdan yeşile döner."}}, "tip": {"en": "Name the test after the behavior, not after the class (test_float_value) — it documents the intent and stays valid when the implementation changes.", "tr": "Testi sınıfa göre değil davranışa göre adlandırın (test_float_value) — niyeti belgelemekle kalmaz, gerçekleştirim değişse de geçerli kalır."}}]});
