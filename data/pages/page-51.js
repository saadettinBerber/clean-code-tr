window.PAGE({"id": "page-51", "page": 51, "pdf_page": 82, "chapter": {"num": 3, "en": "Functions", "tr": "Fonksiyonlar"}, "section": {"en": "SetupTeardownIncluder", "tr": "SetupTeardownIncluder"}, "title": {"en": "Listing 3-7 (continued)", "tr": "Listing 3-7 (devam)"}, "blocks": [{"type": "caption", "kind": "listing", "en": "Listing 3-7 (continued) SetupTeardownIncluder.java", "tr": "Listing 3-7 (devam) SetupTeardownIncluder.java"}, {"type": "code", "lang": "java", "code": "  private void includeSetupPages() throws Exception {\n    if (isSuite)\n      includeSuiteSetupPage();\n    includeSetupPage();\n  }\n\n  private void includeSuiteSetupPage() throws Exception {\n    include(SuiteResponder.SUITE_SETUP_NAME, \"-setup\");\n  }\n\n  private void includeSetupPage() throws Exception {\n    include(\"SetUp\", \"-setup\");\n  }\n\n  private void includePageContent() throws Exception {\n    newPageContent.append(pageData.getContent());\n  }\n\n  private void includeTeardownPages() throws Exception {\n    includeTeardownPage();\n    if (isSuite)\n      includeSuiteTeardownPage();\n  }\n\n  private void includeTeardownPage() throws Exception {\n    include(\"TearDown\", \"-teardown\");\n  }\n\n  private void includeSuiteTeardownPage() throws Exception {\n    include(SuiteResponder.SUITE_TEARDOWN_NAME, \"-teardown\");\n  }\n\n  private void updatePageContent() throws Exception {\n    pageData.setContent(newPageContent.toString());\n  }\n\n  private void include(String pageName, String arg) throws Exception {\n    WikiPage inheritedPage = findInheritedPage(pageName);\n    if (inheritedPage != null) {\n      String pagePathName = getPathNameForPage(inheritedPage);\n      buildIncludeDirective(pagePathName, arg);\n    }\n  }\n\n  private WikiPage findInheritedPage(String pageName) throws Exception {\n    return PageCrawlerImpl.getInheritedPage(pageName, testPage);\n  }\n\n  private String getPathNameForPage(WikiPage page) throws Exception {\n    WikiPagePath pagePath = pageCrawler.getFullPath(page);\n    return PathParser.render(pagePath);\n  }\n\n  private void buildIncludeDirective(String pagePathName, String arg) {\n    newPageContent\n      .append(\"\\n!include \")"}], "concepts": [{"id": "composed-method-example", "title": {"en": "Composed Method: Breaking Down Large Methods", "tr": "Oluşturulmuş Metot: Büyük Metotları Küçük Parçalara Bölme"}, "summary": {"en": "Instead of writing one large method that does many things, break it into small, single-purpose private methods that read like a table of contents for the algorithm.", "tr": "Birçok işi birden yapan tek bir büyük metot yazmak yerine, algoritmanın bir içindekiler tablosu gibi okunmasını sağlayan küçük, tek işe odaklanmış private metotlara bölün."}, "bad": {"lang": "python", "code": "def process_order(order):\n    total = 0\n    for item in order.items:\n        total += item.price * item.quantity\n    if order.customer.is_premium:\n        total *= 0.9\n    order.total = total\n    send_email(order.customer.email, \"Order confirmed\", f\"Total: {total}\")\n    order.status = \"confirmed\"\n    save_to_database(order)", "why": {"en": "This single method calculates totals, applies discounts, sends email, and persists data all at once — its name promises one thing but it does five.", "tr": "Bu tek metot; toplamı hesaplıyor, indirim uyguluyor, e-posta gönderiyor ve veriyi kaydediyor — adı tek bir şey vaat ediyor ama beş iş yapıyor."}}, "good": {"lang": "python", "code": "def process_order(order):\n    calculate_total(order)\n    apply_premium_discount(order)\n    confirm_order(order)\n\ndef calculate_total(order):\n    order.total = sum(item.price * item.quantity for item in order.items)\n\ndef apply_premium_discount(order):\n    if order.customer.is_premium:\n        order.total *= 0.9\n\ndef confirm_order(order):\n    order.status = \"confirmed\"\n    notify_customer(order)\n    save_to_database(order)\n\ndef notify_customer(order):\n    send_email(order.customer.email, \"Order confirmed\", f\"Total: {order.total}\")", "why": {"en": "process_order now reads like a summary; each private method has one job and one reason to change, exactly like includeSetupPages() delegating to includeSuiteSetupPage() and includeSetupPage().", "tr": "process_order artık bir özet gibi okunuyor; her private metodun tek bir işi ve değişmesi için tek bir nedeni var — tıpkı includeSetupPages()'in includeSuiteSetupPage() ve includeSetupPage()'e iş devretmesi gibi."}}, "tip": {"en": "If you can't summarize a method in one sentence without using \"and\", it's doing more than one thing — extract it.", "tr": "Bir metodu \"ve\" kelimesini kullanmadan tek cümleyle özetleyemiyorsanız, o metot birden fazla iş yapıyor demektir — parçalara ayırın."}}, {"id": "descriptive-names-example", "title": {"en": "Descriptive Names Make Code Self-Documenting", "tr": "Betimleyici İsimler Kodu Kendini Anlatır Hale Getirir"}, "summary": {"en": "A well-named function needs no comment to explain what it does — the name itself states the intent, just as includeSuiteTeardownPage() or buildIncludeDirective() do.", "tr": "İyi isimlendirilmiş bir fonksiyon ne yaptığını açıklamak için yoruma ihtiyaç duymaz — isim, tıpkı includeSuiteTeardownPage() veya buildIncludeDirective() gibi niyeti kendisi anlatır."}, "bad": {"lang": "javascript", "code": "// check if user can do the thing\nfunction check(u, t) {\n  return u.role === \"admin\" || u.perms.includes(t);\n}", "why": {"en": "The comment exists only because the names \"check\", \"u\", and \"t\" reveal nothing; the vagueness of the names is what forces the comment to exist.", "tr": "Yorum yalnızca \"check\", \"u\" ve \"t\" isimleri hiçbir şey açıklamadığı için var; yorumu zorunlu kılan şey isimlerdeki bu belirsizlik."}}, "good": {"lang": "javascript", "code": "function userIsAuthorizedFor(user, task) {\n  return user.role === \"admin\" || user.permissions.includes(task);\n}", "why": {"en": "userIsAuthorizedFor(user, task) reads as a question and answers it — no comment needed, and a reviewer instantly knows what calling it means.", "tr": "userIsAuthorizedFor(user, task) bir soru gibi okunur ve o soruyu yanıtlar — yoruma gerek kalmaz, kodu inceleyen kişi çağrının ne anlama geldiğini anında anlar."}}, "tip": {"en": "Spend the extra ten seconds naming a function well; it pays back every time someone else (including future you) reads it.", "tr": "Bir fonksiyona iyi isim vermek için harcanan on saniye fazladan, başka biri (gelecekteki siz dahil) o kodu her okuduğunda geri öder."}}]});
